# Spec coverage — claim inventory and verdicts

**Source:** `Docs/voice/webrtc-voice-spec.md`, "Spatial WebRTC Voice Service — Feature Specification",
written 2026-08-15.
- **Pass 0:** enumerated the spec's normative claims.
- **Pass 0b (2026-09-14):** applied John's flag rulings.
- **Pass 1a (2026-09-14):** added a Verdict and an Evidence column for the rows in §1, §3 and §4
  (SC-3 – SC-55). The other rows' verdict cells stay blank until later passes.
- **Pass 1b (2026-09-14):** filled Verdict and Evidence for the rows in §5 and §6 (SC-56 – SC-77) under
  the same rules.
- **Pass 1c (2026-09-14):** filled Verdict and Evidence for the rows in §7 (SC-78 – SC-105). It also
  filled SC-141 (§9), which shares SC-88's code path.
- **Pass 1d (2026-09-14):** filled Verdict and Evidence for §8 and §9 (SC-106 – SC-140). This is the final
  survey pass. SC-1 and SC-2 (the header scope line) were in none of the passes' ranges and had no
  verdict until the spec amendment.
- **Spec amendment (2026-09-14):** added the verdict `DESCOPED` for features deliberately not built, and
  applied it to 29 rows. SC-68 became `MET` with its unbuilt half descoped, and SC-1 and SC-2 were
  verdicted. The spec itself now marks each descoped item and carries a status block. See "Spec
  amendment (2026-09-14)" under the rules.

## Header

- **Length:** `wc -l Docs/voice/webrtc-voice-spec.md` → `198`.
- **Tracking:** tracked in git. `git log --follow` shows one commit, `2a592f86e3` (2026-08-31 06:03 -0500,
  `docs(voice): O-28 — resync Docs/voice with the mixer (sim side)`). `git blame` attributes all 198
  lines to `2a592f86e3`. The 2026-08-15 authoring date therefore does not appear in this repository's
  history, which starts at the O-28 mirror resync. The mixer copy
  (`legion-voice-mixer/docs/voice/webrtc-voice-spec.md`) is byte-identical (SHA-256 `4F1F9904…4433A`).
- **Line numbers:** taken from the file at tranq-ais `feature/ais-v3` `a8b85fbcdd`.

### Table of contents of the spec

| Line | Heading |
|---|---|
| 1 | # Spatial WebRTC Voice Service — Feature Specification |
| 8 | ## 1. Goals and non-goals |
| 27 | ## 2. Requirements provenance |
| 37 | ## 3. Tier 0 — Trust and privacy invariants |
| 41 | ### 3.1 All media is server-relayed, always **[OPS]** |
| 45 | ### 3.2 Voice-server trust domains **[OPS]** |
| 53 | ### 3.3 Sim-authoritative permission enforcement **[ENG]** |
| 57 | ### 3.4 Mixer output is the permission boundary **[ENG]** |
| 61 | ### 3.5 Capture requires consent **[OPS]** |
| 67 | ## 4. Tier 1 — Reliability and diagnosability |
| 71 | ### 4.1 Per-connection state vector |
| 79 | ### 4.2 Built-in echo test |
| 83 | ### 4.3 Fail loud |
| 87 | ### 4.4 Session event log |
| 91 | ### 4.5 Fleet observability (activates at scale) |
| 97 | ## 5. Tier 2 — Media plane quality |
| 110 | ## 6. Tier 3 — Spatial engine |
| 123 | ## 7. Tier 4 — Features |
| 125 | ### 7.1 Camera-position listening **[REC]** |
| 132 | ### 7.2 Performer mode **[REC]** |
| 136 | ### 7.3 Parcel and estate voice zones **[OPS]** |
| 140 | ### 7.4 Voice morphing |
| 150 | ### 7.5 Connector layer **[OPS]** |
| 158 | ### 7.6 Moderation surface **[OPS]** |
| 164 | ## 8. Deployment and scaling model |
| 168 | ### 8.1 Small (1 region – ~20 regions) |
| 172 | ### 8.2 Medium (grid, tens–hundreds of regions) |
| 176 | ### 8.3 Large (InWorldz/SL scale) |
| 186 | ## 9. Protocol compatibility summary |
| 193 | ## 10. Open questions |

### Rules applied

- **One row per claim:** every "will / must / provides / supports" statement, named feature, or committed
  behaviour gets its own row. The header scope line (line 4) and §1 goals and non-goals count.
- **Not inventoried:**
  - §2 (provenance tags, lines 27–33) commits to nothing.
  - §10 lists open questions (lines 195–198), not claims; they are carried verbatim in the appendix.
  - Prose asides that justify a claim without adding one: the §4 and §5 introductions, the CPU and quality
    estimates in §7.4, and the cost remarks in §8.2–§8.3.
- **Scope:** MIXER (the C plugin), SIM (the .cs addon), BOTH, OPS (deployment/config/docs), VIEWER (would
  need viewer changes).
- **Flag** (John's rulings, applied in pass 0b):
  - blank: an ordinary claim for pass 1.
  - `AMENDED`: a later decision changed the mechanism; the invariant still binds, and pass 1 tests the
    amended form.
  - `BLOCKED-BY-CONSTRAINT`: satisfying the claim would break a standing project constraint.
  - `DERIVED`: an umbrella goal with no separate finding of its own; it follows from the claims named in
    its ruling.
  - `NOT-A-COMMITMENT`: the spec's own wording does not commit to it.

  The flag rulings table below gives each ruling's reason and, where one was set, the criterion pass 1
  must apply.
- **Wording:** claims follow the spec's own words. Three claims (SC-19, SC-92, SC-115) were lightly
  reworded in pass 0; see the wording notes below the rulings table.

### Pass 1a verdicts (§1, §3, §4)

- **Verdict:**
  - `MET`: the claim is satisfied.
  - `PARTIAL`: some of it is satisfied; the evidence names the part that is not.
  - `UNMET`: the claim is not satisfied.
  - `BLOCKED`: it cannot be satisfied under a standing constraint, which the evidence names.
  - `DERIVED`: no independent verdict; the evidence names the rows it derives from.
  - `UNKNOWN`: not determined within budget (no row in pass 1a).
- **Source trees checked:** tranq-ais `feature/ais-v3` at `3bf4f92b7c` and legion-voice-mixer `main` at
  `4198951`, read only.
- **Citations:**
  - `SIM <path>:<line>` is relative to `D:\tranq-ais`; `MIXER <path>:<line>` is relative to
    `D:\legion-voice-mixer`. A bare `:<line>` repeats the file cited just before it.
  - Every cited line was opened and matched against the evidence text before this pass was committed.
  - Only source is cited as evidence: code, config, scripts. Some citations point at a source comment
    stating a design limit; those are labelled "(comment)".
- **Searches:** each `UNMET` row gives the literal `git grep` it rests on and its result. Inside table
  cells the regex alternation bar is escaped as `\|`; the command itself uses a plain `|`.
- **Criteria:** where the pass-0b rulings set a pass-1 criterion, it was applied literally. SC-32 reports
  its two paths separately.

### Pass 1b verdicts (§5, §6)

- **Rules:** the pass 1a verdict vocabulary, citation format and search rules above apply unchanged.
  There is no `UNKNOWN` row in pass 1b.
- **Source trees checked:** tranq-ais `feature/ais-v3` at `cf98cdcb0e` and legion-voice-mixer `main` at
  `4198951`, read only. Every row in scope is MIXER.
- **Criteria:**
  - §5 fixes 20 ms frames. Where §6 says "10 ms frames" (SC-72), it is read as "several frames" per the
    pass-0b ruling.
  - SC-58 is tested against the configured or negotiated Opus ingest floor, not an intent.
- **Vacuous MET:** SC-73 is `MET` only because the binning path it constrains does not exist; its
  evidence says so.

### Pass 1c verdicts (§7)

- **Rules:** the pass 1a verdict vocabulary, citation format and search rules apply unchanged, with one
  addition below. There is no `UNKNOWN` row in pass 1c.
- **Source trees checked:** tranq-ais `feature/ais-v3` at `539b4699fa` and legion-voice-mixer `main` at
  `4198951`, read only.
- **Rulings applied:**
  - SC-83 is `BLOCKED` under the stock-viewer constraint.
  - SC-85 and SC-95 are judged against the amended mechanism in their flag-ruling rows.
- **`NOT-A-COMMITMENT` verdict:** SC-98 carries the pass-0b ruling of the same name as its verdict. This
  records that no finding is owed, not that the claim is satisfied. Its evidence still records a
  search. This is the only verdict term pass 1c adds.
- **Parcel crossing (SC-88):** tested by the code path that picks a participant's room on a crossing.
  - SC-141 (§9) is the same claim and was filled in this pass with the same result.
  - It is the only row outside §7 in pass 1c.
- **§7.4 searches:** the searches establishing that no voice morphing exists are listed once, in SC-89;
  SC-90 – SC-94 refer to them.

### Pass 1d verdicts (§8, §9)

- **Rules:** the pass 1a – 1c vocabulary, citation format and search rules apply unchanged. There is no
  `UNKNOWN` row in pass 1d.
- **Source trees checked:** tranq-ais `feature/ais-v3` at `bcf6e45aef` and legion-voice-mixer `main` at
  `4198951`, read only. SC-133 also cites the pinned Janus submodule under `vendor/janus-gateway`.
- **Docs as evidence:** for OPS-scope rows, a doc may be cited for configuration and install steps and
  is labelled "(doc)". It is never cited as evidence that code exists.
- **Install cost (SC-108, SC-115):** counted against what working voice with the Legion mixer needs
  today:
  - INI sections and keys the operator must set;
  - mixer env vars with no working default;
  - manual steps;
  - the container runtime.
- **VIEWER row:** SC-127 is `BLOCKED` under the stock-viewer constraint, as the pass 1a VIEWER rows were.
- **Vacuous MET:** SC-110 is `MET` only because there is no separate small mode to differ from; its
  evidence says so.
- **SC-138:** reports the configured cadence, not the spec's intent.

### Spec amendment (2026-09-14)

- **New verdict `DESCOPED`:** the feature was deliberately not built. It is a decision dated 2026-09-14,
  not a finding, and each such row says so.
  - The row keeps its earlier pass's finding after the decision note, for the record.
  - A later pass does not re-verdict it unless the decision is reversed.
- **Reasons:**
  - scale tier: not needed for the target deployment;
  - deferred DSP: deferred by decision;
  - features: not built.
- **Rows descoped (29):**
  - §5/§6 deferred DSP: SC-65, SC-70, SC-71, SC-72, SC-74, SC-75, SC-77;
  - §7.2 performer mode: SC-81, SC-82, SC-84 (SC-83 stays `BLOCKED`);
  - §7.4 voice morphing: SC-89 – SC-94;
  - §7.5: SC-97 (transcription), SC-101 (effects sends);
  - §7.6: SC-103 (podium mode);
  - scale tier: SC-119, SC-120, SC-122, SC-123, SC-125, SC-126, SC-128, SC-129, SC-130, SC-132.
- **Part-descoped rows:** where only part of a row was descoped, the verdict covers the rest of the row,
  and the evidence names the descoped part.
  - SC-68 is `MET`: its load-adaptive aggregation is descoped, and culling and per-listener rendering
    were met in pass 1b.
  - SC-2 is `MET`: its large multi-mixer end is descoped.
- **Mismatches with the descoping decision:**
  - It listed "placement map" and "co-location" under §8.3. Those rows are SC-119 and SC-120 in §8.2,
    and each name matches only that row, so both are descoped and their rows say so.
  - It said "all of §8.3", but four §8.3 rows are not among the items it named, so they are unchanged:
    SC-124 (admission control, `PARTIAL`), SC-127 (viewer-side FOA decode, `BLOCKED`), SC-131
    (rate-limited provisioning, `PARTIAL`) and SC-133 (RTP source validation, `MET`).
- **Related rows left unchanged:** SC-9 (§1 goal, voice morphing), the effects half of SC-11 (§1 goal),
  SC-73 (vacuous `MET` on binning) and the morph-state part of SC-139 (§9). The decision did not name
  them.
- **SC-1 and SC-2:** verdicted under the pass 1a rules against tranq-ais `feature/ais-v3` at
  `7d018bd3a6` and legion-voice-mixer `main` at `bedca0b`. SC-2 is an OPS row, so a doc is cited for
  configuration.

## Inventory

| ID | Spec § | Line | Claim | Scope | Flag | Verdict | Evidence |
|---|---|---|---|---|---|---|---|
| SC-1 | Header | 4 | A server-side WebRTC voice service (mixer plugin + region/grid integration) for the published Second Life WebRTC voice protocol, for OpenSimulator-derived grids | BOTH | | MET | **Mixer plugin:** `MIXER src/janus_slvoice.c:89` "#define JANUS_SLVOICE_PACKAGE         "janus.plugin.slvoice"", registered with the Janus core at `:282` ".get_package = janus_slvoice_get_package,". **Region integration (the SL protocol caps):** `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:339` "caps.RegisterSimpleHandler("ProvisionVoiceAccountRequest"," and `:345` "caps.RegisterSimpleHandler("VoiceSignalingRequest",". **Grid integration (the Robust-side service):** `SIM Addons/os-webrtc-janus/WebRtcVoice/WebRtcVoiceServerConnector.cs:47` "public class WebRtcVoiceServerConnector : IServiceConnector". How completely the protocol is implemented is a separate claim: SC-3, PARTIAL. |
| SC-2 | Header | 4 | Designed to run unchanged from a single-region standalone to a large multi-mixer grid | OPS | | MET | **The large multi-mixer end is DESCOPED 2026-09-14 (the scale tier is not needed for the target deployment; a decision, not a finding).** The verdict covers the rest: one code path and configuration model from a single-region standalone to a grid-service deployment. **Chosen by configuration:** `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:94` "moduleConfig.GetString("SpatialVoiceService", string.Empty);", and loaded by name at `:112` "ServerUtils.LoadPlugin<IWebRtcVoiceService>(spatialDllName, [m_Config]);". **Either the region's own Janus service or the grid connector:** `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:7` "; SpatialVoiceService=WebRtcVoice.dll:WebRtcVoiceServiceConnector". **Served on Robust by:** `SIM Addons/os-webrtc-janus/WebRtcVoice/WebRtcVoiceServerConnector.cs:58` "IConfig moduleConfig = pConfig.Configs["WebRtcVoice"];". Its Robust configuration is documented at `SIM Addons/os-webrtc-janus/README.md:201` (doc; OPS row). |
| SC-3 | 1 | 12 | Full compatibility with the SL WebRTC viewer protocol (ProvisionVoiceAccountRequest / VoiceSignalingRequest caps, SLData data channel) | BOTH | | PARTIAL | **Met:** both caps are registered: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:328` "RegisterSimpleHandler("ProvisionVoiceAccountRequest"" and `:334` "RegisterSimpleHandler("VoiceSignalingRequest"". Trickle completion is handled: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:507` "TryGetBool("completed", out bool iscompleted)". SLData is parsed, `MIXER src/sldata.c:142` "json_object_get(root, "lp")", and answered with a join presence, `MIXER src/janus_slvoice.c:852` "json_object_set_new(jd, "p", json_true());", and a power batch, `:2585` "json_object_set_new(pv, "p", json_integer(ppow));". **Not met:** group/ad-hoc conference voice. `start conference` is a stub, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/ChatSessionRequestLogic.cs:169` "case MethodStartConference:", under `:167` "Stubs carried over unchanged: 200, no body". A multiagent provision is admitted only for a registered avatar-to-avatar session: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/A2AProvisionAdmission.cs:85` "registry?.TryGetByChannel(channel)". |
| SC-4 | 1 | 12 | Stock WebRTC-capable viewers work without modification | BOTH | | MET | The answer accepts the MID extension a bundled stock viewer requires: `MIXER src/janus_slvoice.c:2022` "JANUS_SDP_OA_ACCEPT_EXTMAP, JANUS_RTP_EXTMAP_MID,". The join presence uses the object shape the viewer parses: `:852` "json_object_set_new(jd, "p", json_true());". The sim advertises STUN to the viewer: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:236` "AddFeature("stun-servers", OSD.FromString(m_StunServers))", set by default in `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:26` "StunServers = stun:stun.l.google.com:19302". Source review only; no live viewer was run in this pass. |
| SC-5 | 1 | 13 | True per-listener spatial audio (distance, azimuth, listener orientation) | MIXER | | MET | **Distance** per listener: `MIXER src/janus_slvoice.c:3143` "slv_vec3_mag(slv_vec3_sub(s->snap_lp, sess[j]->snap_sp))", with falloff at `:3154` "gains[j] *= (float)pow(t, slv_spatial.falloff_exp);". **Azimuth** in the listener's orientation: `:3162` "slv_azimuth(s->snap_lp, s->snap_lh, sess[j]->snap_sp)". The heading quaternion sets the forward vector: `MIXER src/mixer/azimuth.h:57` "double fy = 2.0 * (q.x * q.y + q.w * q.z);". **Pan:** constant-power, `MIXER src/mixer/pan.h:53` "double p = (sin(azimuth) + 1.0) / 2.0;". |
| SC-6 | 1 | 14 | Privacy and permission enforcement performed in the mix, on the server, not delegated to clients | BOTH | | MET | **Sim decides:** estate ban, `SIM Addons/os-webrtc-janus/Visibility/VisibilityRules.cs:27` "estate.IsEstateBanned(source.Id)"; parcel ban, `:41` "sourceParcel.ExcludesByBan(listener.Id)". It pushes the result: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/JanusPeerCtlBatchSink.cs:219` "PeerCtlBatchSerializer.BuildRequest(op, exclSlice, muteSlice);". **Mixer enforces** it in the mix, `MIXER src/janus_slvoice.c:3125` "if(slv_roster_excludes(s->excluded, disp))" and `:3110` "g_hash_table_contains(s->mod_muted, disp)", and in the roster, `:929` "if(slv_roster_excludes(listener->excluded, p->display))". |
| SC-7 | 1 | 15 | First-class diagnosability: voice failures must be triageable by the user or operator in minutes, without log-emailing rituals | BOTH | DERIVED | DERIVED | Derives from SC-41, SC-52 and SC-53 (pass-0b ruling). |
| SC-8 | 1 | 16 | Restoration of camera-position listening, lost in the Vivox→WebRTC transition | BOTH | | MET | The listener position is the viewer's camera `lp`: `MIXER src/janus_slvoice.c:2972` "s->snap_lp = s->last_data.lp;". The server applies a leash around the avatar: `:2980` "s->snap_lp = slv_vec3_madd(s->snap_sp, slv_spatial.leash_dist / d, off);". Distance and pan are taken from `lp`: `:3143` "slv_vec3_sub(s->snap_lp, sess[j]->snap_sp)" and `:3162` "slv_azimuth(s->snap_lp, s->snap_lh, sess[j]->snap_sp)". |
| SC-9 | 1 | 16 | Restoration of voice morphing, lost in the Vivox→WebRTC transition | MIXER | | UNMET | `git -C D:/legion-voice-mixer grep -n -i -E 'morph\|pitch.?shift\|formant\|psola\|vocoder\|voice.?font' -- src connectors etc docker-entrypoint.sh` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'morph\|pitch.?shift\|formant\|psola\|vocoder\|voice.?font' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-10 | 1 | 16 | A capability Vivox never had: mixer-enforced moderation | BOTH | | MET | **Operator mute:** `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceModerationCommands.cs:89` "AddCommand("Voice", false, "voice moderation mute",". The matrix emits a mute channel, `SIM Addons/os-webrtc-janus/Visibility/VisibilityMatrix.cs:84` "VisibilityRules.IsModerationMuted(agents[si], parcels[si])", sent in the batch at `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/JanusPeerCtlBatchSink.cs:219` "BuildRequest(op, exclSlice, muteSlice);". **Mixer:** applies it, `MIXER src/janus_slvoice.c:1838` "janus_slvoice_set_mod_muted_locked(L, e->excl[m], TRUE)", and silences the source in the mix, `:3110` "g_hash_table_contains(s->mod_muted, disp)" → `:3111` "mutes[j] = 1;". |
| SC-11 | 1 | 16 | A capability Vivox never had: effects/connector hooks | MIXER | | PARTIAL | **Met — connector hooks:** send hook `MIXER connectors/common/peer.py:57` "def local_track(self):"; injector `MIXER connectors/injector/injector.py:208` "def local_track(self):"; recorder `MIXER connectors/recorder/recorder.py:49` "def on_audio_track(self, track) -> None:"; sim policy record `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceConnector/VoiceConnectorRecord.cs:59` "public bool MayInject { get; }". **Not met — effects:** `git -C D:/legion-voice-mixer grep -n -i -E 'reverb\|biquad\|equali[sz]\|distortion\|chorus\|flanger\|dsp.?chain\|audio.?filter\|voice.?effect' -- src connectors etc` → 0 matches; the same pattern over `git -C D:/tranq-ais … -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-12 | 1 | 17 | Scale-invariant deployment: identical code path and configuration model from a one-region hobby grid to a large commercial grid | OPS | | PARTIAL | **Met — one configuration model:** the same service class loads in the region or behind the grid service, `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:112` "ServerUtils.LoadPlugin<IWebRtcVoiceService>(spatialDllName, [m_Config]);"; one env file, `MIXER docker-compose.yml:61` "env_file:". **Not met — large-grid scale-out:** each service has a single mixer endpoint, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:105` "janusConfig.GetString("JanusGatewayURI", string.Empty);"; rooms are capped per mixer, `MIXER src/janus_slvoice.c:164` "#define SLV_MAX_MIX         110". No sharding: `git -C D:/tranq-ais grep -n -i -E 'shard\|load.?balanc\|GatewayURIs\|MixerPool\|mixer.?pool' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches; `git -C D:/legion-voice-mixer grep -n -i -E 'shard\|load.?balanc\|mixer.?pool\|replica' -- src docker-compose.yml env.sample docker-entrypoint.sh etc` → 0 matches. |
| SC-13 | 1 | 17 | Small deployments must not pay complexity for scale they don't need | OPS | | MET | One prebuilt image, `MIXER docker-compose.yml:14` "image: ghcr.io/johnlegionh/legion-voice-mixer:latest", configured from one env file, `:61` "env_file:" / `:62` "- .env". The operator values are the secrets and public address, `MIXER env.sample:28` "JS_API_SECRET=", `:30` "JS_ADMIN_SECRET=", `:6` "JS_PUBLIC_IP="; everything else has defaults, e.g. `MIXER docker-entrypoint.sh:28` ": "${JS_HTTP_PORT:=14223}"". The Janus config is generated from them: `:242` "set_kv "$HTTP_JCFG" port            "${JS_HTTP_PORT}"". |
| SC-14 | 1 | 21 | Non-goal: client-side spatialization via selective forwarding (SFU); ruled out by the privacy model (§3) | MIXER | | MET | No SFU: inbound RTP only enters a jitter buffer, `MIXER src/janus_slvoice.c:3352` "janus_slvoice_jb_insert(session, seq, payload, plen);". Each listener gets a server-built N-minus-one mix, `:3175` "summed = slv_mix_nminus1_stereo(frame, SLV_FRAME_TOTAL, srcbuf, audible,", and only that mix is relayed, `:2949` "gateway->relay_rtp(s->handle, &outp);". |
| SC-15 | 1 | 22 | Non-goal: true peer-to-peer media between clients; ruled out permanently (§3.1) | BOTH | | MET | Every viewer session, including avatar-to-avatar calls, is placed in a mixer room: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:408` "viewerSession.Room = await viewerSession.AudioBridge.SelectRoom(pSceneID.ToString(),". The mixer is the media endpoint: inbound RTP is jitter-buffered at `MIXER src/janus_slvoice.c:3352` "janus_slvoice_jb_insert(session, seq, payload, plen);", and only the mix goes out, `:2949` "gateway->relay_rtp(s->handle, &outp);". |
| SC-16 | 1 | 23 | Non-goal: video | MIXER | | MET | The answer is generated with every m-line rejected by default, `MIXER src/janus_slvoice.c:2008` "janus_sdp *answer = janus_sdp_generate_answer(offer);", with only audio and application accepted afterwards. Video RTP is dropped: `:3338` "if(packet == NULL \|\| packet->buffer == NULL \|\| packet->video)". |
| SC-17 | 3.1 | 43 | No client ever receives another client's ICE candidates | BOTH | | MET | A viewer's candidates go only to its own Janus session: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:545` "viewerSession.Session.TrickleCandidates(viewerSession, candidatesArray)". Janus-to-sim trickle is raised as an event, `SIM Addons/os-webrtc-janus/Janus/JanusSession.cs:634` "OnTrickle?.Invoke(eventResp);", that nothing subscribes to: `git -C D:/tranq-ais grep -n -i -E 'OnTrickle' -- Addons/os-webrtc-janus ':!*.md'` → 3 matches, the declaration (:566), the reset (:580) and the invoke (:634). The mixer never handles candidates: `git -C D:/legion-voice-mixer grep -n -i -E 'candidate\|trickle' -- src` → 0 matches. |
| SC-18 | 3.1 | 43 | Every media connection is client ↔ voice server | BOTH | | MET | The sim joins the viewer to a mixer room, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:419` "var joinResult = await viewerSession.Room.JoinRoom(viewerSession)", and returns the mixer's answer, `SIM Addons/os-webrtc-janus/Janus/ProvisionResponseBuilder.cs:25` "{ "jsep", jsepAnswer },". The mixer writes that answer, `MIXER src/janus_slvoice.c:2369` "jsep_answer = json_pack("{ssss}", "type", "answer", "sdp", answer_sdp);", and relays media only on the listener's own handle, `:2949` "gateway->relay_rtp(s->handle, &outp);". |
| SC-19 | 3.1 | 43 | "Peer-to-peer" calls are two-party sessions on the non-spatial pool, exactly as Second Life does | BOTH | | PARTIAL | **Met — sim side:** admission requires the caller to be a party, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/A2AProvisionAdmission.cs:88` "if (!s.IsParty(agentID))", and a session has exactly two, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/A2ASessionRegistry.cs:80` "agent == Caller \|\| agent == Callee". Non-local provisions go to the non-spatial service, `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:565` "m_nonSpatialVoiceService.CreateViewerSession(pRequest, pUserID, pSceneID);", and the room is created non-spatial, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:394` "bool isSpatial = channel_type == "local";". **Not met:** (1) the mixer stores the flag, `MIXER src/janus_slvoice.c:590` "room->spatial_audio = spatial;", but the tick never reads it (`git -C D:/legion-voice-mixer grep -n -E 'spatial_audio' -- src` → 5 matches: :324 field, :590 set, :1013 config, :2149 create, :2433 list output), so the distance cull still runs in these rooms, `:3144` "if(janus_slvoice_distance_cull_locked(s, disp, dcull, room->tick_seq))". (2) The mixer has no two-party limit, only the general cap at `:2262` "if(room_pop >= SLV_MAX_MIX)". (3) Without a configured non-spatial service the sim falls back to the spatial one, `WebRtcVoiceServiceModule.cs:138` "m_nonSpatialVoiceService = m_spatialVoiceService;". "Exactly as Second Life does" cannot be checked from source. |
| SC-20 | 3.1 | 43 | Client IP addresses are never exposed to other users | BOTH | | MET | The participant list carries only the agent UUID: `MIXER src/janus_slvoice.c:2092` "json_object_set_new(pl, "display", json_string(p->display));". No address handling in either tree: `git -C D:/legion-voice-mixer grep -n -i -E 'inet_ntop\|getpeername\|remote_ip\|ip_address\|"ip"' -- src` → 0 matches; `git -C D:/tranq-ais grep -n -i -E 'RemoteEndPoint\|ClientIP\|IPAddress' -- Addons/os-webrtc-janus ':!*Test*' ':!*.md'` → 0 matches. Candidates are not relayed to other clients (see SC-17's source lines). |
| SC-21 | 3.1 | 43 | Structurally impossible to misconfigure: there is no configuration option that enables direct client-to-client ICE | BOTH | | MET | The only ICE settings are server-side NAT settings, `MIXER docker-entrypoint.sh:223` "ensure_kv_in_section "$JANUS_JCFG" nat nat_1_1_mapping" and `:224` "nat keep_private_host", plus the sim's STUN list, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:236` "AddFeature("stun-servers", OSD.FromString(m_StunServers))". No peer-to-peer option: `git -C D:/tranq-ais grep -n -i -E 'p2p_?ice\|direct.?ice\|peer.?to.?peer\|client.?to.?client' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches; `git -C D:/legion-voice-mixer grep -n -i -E 'p2p_?ice\|direct.?ice\|peer.?to.?peer\|client.?to.?client' -- src connectors docker-entrypoint.sh env.sample etc` → 0 matches. |
| SC-22 | 3.2 | 47 | Every voice server instance is classified as grid-operated or region-operated | OPS | | UNMET | `git -C D:/tranq-ais grep -n -i -E 'trust.?domain\|grid.?operated\|region.?operated\|operator.?class\|TrustClass' -- Addons/os-webrtc-janus Source ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'trust.?domain\|grid.?operated\|region.?operated\|operator.?class' -- src connectors docker-compose.yml docker-entrypoint.sh env.sample etc ':!*.md'` → 0 matches. |
| SC-23 | 3.2 | 47 | The trust-domain classification is communicated to the client at provisioning time | SIM | | UNMET | Criterion: the provisioning response carries no trust-domain field. `git -C D:/tranq-ais grep -n -i -E 'trust.?domain\|grid.?operated\|region.?operated\|operator.?class\|TrustClass' -- Addons/os-webrtc-janus Source ':!*.md'` → 0 matches. The success response carries only three keys: `SIM Addons/os-webrtc-janus/Janus/ProvisionResponseBuilder.cs:25` "{ "jsep", jsepAnswer },", `:26` "{ "viewer_session", viewerSessionId },", `:27` "{ "room", room }". |
| SC-24 | 3.2 | 49 | Hypergrid visitors are provisioned only onto grid-operated servers | SIM | | UNMET | No hypergrid check on the provisioning path: `git -C D:/tranq-ais grep -n -i -E 'IsLocalUser\|IsForeign\|foreign\|HGVisitor\|ViaHG\|HomeURI\|IsLocalGridUser\|ServiceURLs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. No server classification to route to either (SC-22's searches → 0 matches). |
| SC-25 | 3.2 | 49 | Region-local voice is not offered to hypergrid visitors | SIM | | UNMET | `git -C D:/tranq-ais grep -n -i -E 'IsLocalUser\|IsForeign\|foreign\|HGVisitor\|ViaHG\|HomeURI\|IsLocalGridUser\|ServiceURLs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. Local provisioning checks only estate voice, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:636` "if (!scene.RegionInfo.EstateSettings.AllowVoice)", and parcel ban/restrict, `:732` "if(parcel.IsRestrictedFromLand(agentID) \|\| parcel.IsBannedFromLand(agentID))". |
| SC-26 | 3.2 | 50 | Local users connecting to region-operated servers receive a disclosure | BOTH | | UNMET | Criterion: nothing on the provisioning path emits disclosure text or a flag. `git -C D:/tranq-ais grep -n -i -E 'disclos' -- Addons/os-webrtc-janus/Janus Addons/os-webrtc-janus/WebRtcVoice Addons/os-webrtc-janus/WebRtcVoiceServiceModule Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'disclos' -- src etc docker-entrypoint.sh` → 0 matches. The only disclosure code is the connector notice (SC-36), which has nothing to do with server type. |
| SC-27 | 3.2 | 51 | Default topology is grid-operated | OPS | | UNMET | No grid-operated classification exists: `git -C D:/tranq-ais grep -n -i -E 'trust.?domain\|grid.?operated\|region.?operated\|operator.?class\|TrustClass' -- Addons/os-webrtc-janus Source ':!*.md'` → 0 matches. The shipped default points the region at its own Janus: `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini:5` "SpatialVoiceService = WebRtcJanusService.dll:WebRtcJanusService", described at `:4` "a Janus service just for spatial voice for this region". |
| SC-28 | 3.2 | 51 | Region-local is an opt-in for closed estates that want the latency benefit | OPS | | UNMET | `git -C D:/tranq-ais grep -n -i -E 'closed.?estate\|RegionOperated\|GridOperated' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'closed.?estate\|region.?operated\|grid.?operated' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample` → 0 matches. |
| SC-29 | 3.3 | 55 | Parcel and estate audibility is always computed from the avatar's authoritative sim-side position and the sim's access lists | SIM | | PARTIAL | **Met — visibility feeder and root-agent provisioning.** The feeder reads the sim's position, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/FeederWorldFromScene.cs:66` "sp.UUID, sp.IsChildAgent, sp.AbsolutePosition, sp.currentParcelUUID", looks up the parcel from it, `SIM Addons/os-webrtc-janus/Visibility/VisibilityMatrix.cs:101` "return world.GetParcelAt(a.Position);", and reads the live ban list, `FeederWorldFromScene.cs:122` "banned = parcel.IsBannedFromLand;". Root-agent provisioning uses the sim position: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:673` "GetLandObject(sp.AbsolutePosition.X, sp.AbsolutePosition.Y)". **Not met — child agents** (neighbour-region provisions): the viewer's `parcel_local_id` selects the parcel the checks run against, `:675` "if (res.Source == ParcelSource.ClientHint)" → `:676` "GetLandObject(res.LocalId.Value)", set from the client at `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/ProvisionParcelResolver.cs:53` "new ParcelResolution(ParcelSource.ClientHint, LocalId: client". |
| SC-30 | 3.3 | 55 | Access/visibility state is pushed from the simulator to the mixer with version epochs | BOTH | | UNMET | Criterion: the sim-to-mixer batch carries no version epoch. The mixer parses only `op`, `room`, `excl` and `mute`: `MIXER src/visbatch.c:149` "json_object_get(b, "op")", `:167` "json_object_get(b, "room")", `:175` "json_object_get(b, "excl")", `:182` "json_object_get(b, "mute")". `git -C D:/legion-voice-mixer grep -n -i -E 'epoch\|version\|\bseq' -- src/visbatch.c src/visbatch.h` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'epoch' -- Addons/os-webrtc-janus/Visibility/PeerCtlBatchSerializer.cs Addons/os-webrtc-janus/WebRtcVoiceRegionModule/JanusPeerCtlBatchSink.cs` → 0 matches. Two epoch-named counters exist but neither goes on the wire: the mixer's local count of applied batches, `MIXER src/janus_slvoice.c:1735` "room->vis_epoch++;", and the sim's send-lock token, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VisibilityBatchSender.cs:66` "private long _sendEpochSeq;". |
| SC-31 | 3.3 | 55 | Access/visibility state fails closed on staleness | BOTH | | UNMET | Criterion: with stale or absent state the mixer **passes** audio. Code path: a session's exclusion set starts empty, `MIXER src/janus_slvoice.c:1315` "session->excluded = g_hash_table_new_full(g_str_hash, g_str_equal, g_free, NULL);". The check excludes only what is in the set, `MIXER src/roster.h:27` "g_hash_table_contains(excluded, uuid)". The tick silences a source only on that check, `MIXER src/janus_slvoice.c:3125` "if(slv_roster_excludes(s->excluded, disp))" → `:3126` "mutes[j] = 1;"; with no batch every source is mixed. The last-batch time is only reported, never acted on: `:1475` "gint64 age_ms = (janus_get_monotonic_time() - qroom->vis_last_ts) / 1000;". `git -C D:/legion-voice-mixer grep -n -i -E 'stale\|fail.?closed\|timeout\|expire' -- src/roster.h src/visbatch.c src/visbatch.h src/deferred.c src/deferred.h` → 0 matches. Sim side: the feeder is off by default, so no batches are sent at all, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:116` "GetBoolean("VisibilityFeederEnabled", false)"; its staleness guard only frees a stuck send, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VisibilityBatchSender.cs:173` "if (elapsed <= _staleThresholdMs)". |
| SC-32 | 3.3 | 55 | Client-reported listener position is accepted only as a rendering hint (§6.1), never as a permission input | BOTH | | PARTIAL | §6.1 read as §7.1, per the pass-0b ruling. Two paths checked separately. **Not met** in both is the child-agent path, which uses the viewer-supplied `parcel_local_id`. **(a) Authorization — PARTIAL.** Root agents use the sim position, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:673` "GetLandObject(sp.AbsolutePosition.X, sp.AbsolutePosition.Y)", and a mismatching client id is only logged (`:703` "does not match the avatar's parcel"). For child agents the client's `parcel_local_id` selects the parcel, `:676` "GetLandObject(res.LocalId.Value)", that the voice-allowed check, `:706` "(land.Flags & (uint)ParcelFlags.AllowVoiceChat) == 0", and the ban/restrict check, `:732` "parcel.IsRestrictedFromLand(agentID)", run against. **(b) Room choice — PARTIAL.** Root agents get the server's parcel id, `:722` "map["parcel_local_id"] = OSD.FromInteger(land.LocalID);". A child agent's client id is forwarded unchanged (`:724` "(comment) the client's parcel_local_id is forwarded unchanged"), read at `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:385` "pRequest.TryGetInt("parcel_local_id", out int pli)", and hashed into the room at `SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:251` "hasher.Add(pParcelLocalID);". **Mixer note:** the client's SLData `lp` drives the distance cull, `MIXER src/janus_slvoice.c:2972` "s->snap_lp = s->last_data.lp;" → `:3144` "janus_slvoice_distance_cull_locked(...)", which can only remove audio. It runs after the sim's exclusion and moderation mute, `:3132` "if(mutes[j])", so it cannot restore a source the sim excluded. |
| SC-33 | 3.4 | 59 | A listener's downstream mix contains only audio that listener is entitled to hear | MIXER | | PARTIAL | **Met while sim state is present:** moderation mute, `MIXER src/janus_slvoice.c:3110` "g_hash_table_contains(s->mod_muted, disp)"; exclusion, `:3125` "if(slv_roster_excludes(s->excluded, disp))"; both honoured by the N-minus-one mix, `:3175` "slv_mix_nminus1_stereo(frame, SLV_FRAME_TOTAL, srcbuf, audible,". Batches that arrived before the join are replayed at join, `:2318` "janus_slvoice_room_replay_deferred_locked(room, session);". **Not met:** before the first batch reaches a listener, the set is empty, `:1315` "session->excluded = g_hash_table_new_full(", and the listener hears every source. With the feeder off, the default at `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:116` "GetBoolean("VisibilityFeederEnabled", false)", no batch ever arrives. |
| SC-34 | 3.4 | 59 | No "muted client-side" audio in flight; a modified viewer cannot un-hide a hidden avatar or un-mute a moderator mute | MIXER | | MET | Hiding and moderation mute are applied server-side inside the per-listener mix: `MIXER src/janus_slvoice.c:3111` "mutes[j] = 1;" (moderation) and `:3126` "mutes[j] = 1;" (exclusion). The viewer's own peer control can only add a mute: `:3114` "if(s->peer_ctl[k].muted)". Excluded sources are also left out of the roster, `:873` "if(slv_roster_excludes(p->excluded, who))", and only the finished per-listener encode is relayed, `:2949` "gateway->relay_rtp(s->handle, &outp);". |
| SC-35 | 3.4 | 59 | The service is an MCU (server mixes) rather than an SFU (server forwards) | MIXER | | MET | Inbound RTP enters a per-session jitter buffer and is not forwarded, `MIXER src/janus_slvoice.c:3352` "janus_slvoice_jb_insert(session, seq, payload, plen);". The server mixes per listener, `:3175` "slv_mix_nminus1_stereo(", re-encodes, `:2926` "int enc_len = opus_encode_float(s->enc, frame, samples, s->outbuf + 12,", and relays the result, `:2949` "gateway->relay_rtp(s->handle, &outp);". |
| SC-36 | 3.5 | 63 | Any tap on a spatial or group mix requires an in-channel disclosure to affected participants | BOTH | | PARTIAL | **Met for connectors registered in the sim:** registration fires the attach notice, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceConnector/VoiceConnectorRegistrar.cs:106` "pDisclosure?.OnAttach(pRecord);"; the region notice text, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceConnector/VoiceConnectorDisclosure.cs:79` "is present in this region's voice."; wired unconditionally, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceConnector/VoiceConnectorModule.cs:157` "m_disclosure = new VoiceConnectorDisclosure(". **Not met:** (1) the mixer admits any `display` and room with no registration check, `MIXER src/janus_slvoice.c:2223` "json_object_get(root, "display")" → `:2280` "session->display = display ? g_strdup(display) : NULL;". The peers take the room straight from env, `MIXER connectors/common/config.py:15` ""room": os.environ.get("ROOM"),", so a tap joined with the API secret gets no disclosure. (2) There is no disclosure path for group or avatar-to-avatar mixes: registration computes only the estate room, `VoiceConnectorModule.cs:281` "int estateRoom = JanusAudioBridge.CalcRoomNumber(". |
| SC-37 | 3.5 | 63 | No plaintext media at rest without explicit configuration and disclosure | OPS | | PARTIAL | **Met — explicit configuration:** the recorder runs only under a compose profile, `MIXER docker-compose.yml:101` "profiles: [recorder]"; the peers refuse to start without their env, `MIXER connectors/common/config.py:21` "sys.exit(f"{prog}: missing required env"; the injector records only with `RECORD=1`, `MIXER connectors/injector/injector.py:52` ""record": os.environ.get("RECORD", "0") == "1",". **Not met:** recordings are plain WAV, `MIXER connectors/common/segments.py:119` "w = wave.open(path, "wb")", and nothing in the recorder or the mixer makes disclosure a precondition of writing (see SC-36's unregistered-tap gap). |
| SC-38 | 3.5 | 63 | The moderation and legal posture ships with the hook, not after the first incident | OPS | | UNMET | Criterion: capture is not consent-gated at the hook. `git -C D:/tranq-ais grep -n -i -E 'consent' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'consent' -- src connectors docker-compose.yml docker-entrypoint.sh env.sample etc ':!*.md'` → 0 matches. The only per-connector gate is injection, not capture: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceConnector/VoiceConnectorRegistrar.cs:93` "if (!pRecord.MayInject)". |
| SC-39 | 4.1 | 73 | The mixer maintains, per connection: ICE state, DTLS state, inbound RTP rate, decode success, active mix memberships, outbound RTP rate, and last client acknowledgment | MIXER | | PARTIAL | **Met — 6 of 7 items**, all in the per-session query: ICE `MIXER src/janus_slvoice.c:1368` "json_object_set_new(info, "ice_state",", DTLS `:1369` "json_object_set_new(info, "dtls_state",", inbound RTP rate `:1386` "json_real(session->rtp_in_rate)", outbound RTP rate `:1387` "json_real(session->rtp_out_rate)", decode success `:1388` "json_object_set_new(info, "decode_ok",", mix memberships `:1446` "json_object_set_new(info, "mix_memberships",". **Not met — last client acknowledgment:** `git -C D:/legion-voice-mixer grep -n -i -E '\back\b\|acknowledg\|last_ack\|client_ack\|last_data_ts\|last_msg_ts\|last_client' -- src` → 0 matches. **Also:** ICE and DTLS are not separate states; both are reported from one up/down flag, `:1367` "(comment) so ICE/DTLS are reported coarsely", and `:1368`–`:1369` both use "up ? "connected" : "disconnected"". |
| SC-40 | 4.1 | 75 | Per-connection state exposed on the console: `voice status <avatar>` on the region/grid console | BOTH | | UNMET | `git -C D:/tranq-ais grep -n -i -E 'voice status' -- Addons Source` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'voice status' -- src connectors docker-entrypoint.sh` → 0 matches. What exists instead: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:598` "AddCommand("Webrtc", false, "janus info"," and `:602` "janus list rooms", which prints per participant only `:667` "{0}/{1},muted={2},talking={3},pos={4}". |
| SC-41 | 4.1 | 76 | Per-connection state exposed on an admin surface: per-region and per-mixer web view | OPS | | UNMET | The criterion is literal: a web view. None exists in either tree. `git -C D:/legion-voice-mixer grep -n -i -E 'text/html\|<html\|status page\|dashboard' -- src etc connectors docker-entrypoint.sh docker-compose.yml Dockerfile` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'text/html\|<html\|status page\|dashboard' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. What exists, which is not a web view: the Janus Admin API's per-session JSON, `MIXER src/janus_slvoice.c:1354` "json_t *janus_slvoice_query_session(janus_plugin_session *handle) {", served because `MIXER docker-entrypoint.sh:244` "set_kv "$HTTP_JCFG" admin_http      true"; and the console commands listed under SC-40. |
| SC-42 | 4.1 | 77 | Per-connection state exposed to the client as a `diag` member pushed on the SLData channel | MIXER | | UNMET | `git -C D:/legion-voice-mixer grep -n -i -E '"diag"' -- src connectors` → 0 matches. The only data-channel sends are the JSON relay helper, `MIXER src/janus_slvoice.c:829` "gateway->relay_data(s->handle, &data);" (presence), and the power/VAD batch, `:2626` and `:2644` "gateway->relay_data(p->handle, &d);". |
| SC-43 | 4.1 | 77 | A viewer can render "connected; receiving 0 pkt/s from server" vs "receiving 340 pkt/s; output device delivered 0 frames", splitting server-fault from device-fault | VIEWER | | BLOCKED | Needs a viewer UI change. Constraint: stock SL/Firestorm viewer compatibility; viewer changes only when unavoidable, and then fork-only. |
| SC-44 | 4.2 | 81 | Every region offers a self-serve echo test | MIXER | | PARTIAL | **Met:** any participant in any mixer room can switch echo on: `MIXER src/janus_slvoice.c:3439` "if(d.fields_seen & SLV_FIELD_ECHO) {" → `:3442` "janus_slvoice_echo_start_locked(session);". **Not met — "self-serve":** the toggle is an SLData extension a stock viewer does not send (`:300` "(comment) which cannot send the {"echo":true} SLData toggle"). The only viewer-independent route is the process-wide autostart, which replaces every participant's mix: `:3322` "if(echo_autostart) {". |
| SC-45 | 4.2 | 81 | The echo test runs on request by SLData message | MIXER | | MET | The field is parsed at `MIXER src/sldata.c:213` "slv_get_bool(json_object_get(root, "echo"), &b)" and acted on at `MIXER src/janus_slvoice.c:3439` "if(d.fields_seen & SLV_FIELD_ECHO) {": start at `:3442` "janus_slvoice_echo_start_locked(session);", stop at `:3447` "janus_slvoice_echo_stop_locked(session);". |
| SC-46 | 4.2 | 81 | The echo test runs on request from a viewer menu | VIEWER | | BLOCKED | A viewer menu item is a viewer change. Constraint: stock SL/Firestorm viewer compatibility; viewer changes only when unavoidable, and then fork-only. |
| SC-47 | 4.2 | 81 | The mixer loops the user's own audio back with ~500 ms delay | MIXER | | MET | `MIXER src/janus_slvoice.c:135` "#define SLV_ECHO_DELAY_MS   500". The ring reads the delayed position, `:2899` "int rp = (wp + SLV_RING_SAMPLES - SLV_DELAY_SAMPLES) % SLV_RING_SAMPLES;", and that frame is sent back to the same participant, `:3173` "janus_slvoice_build_echo(s, frame, SLV_FRAME_SAMPLES);". |
| SC-48 | 4.2 | 81 | The echo test replaces the single grid-wide test region model with a capability available everywhere at negligible cost | OPS | | PARTIAL | **Met — available in every mixer room at negligible cost:** the delay ring is allocated only when echo is first enabled, `MIXER src/janus_slvoice.c:2762` "if(s->ring == NULL)" → `:2763` "s->ring = g_malloc0(". **Not met — as a replacement for a test region:** a user on a stock viewer cannot request it, `:300` "(comment) which cannot send the {"echo":true} SLData toggle". The only other route is the process-wide autostart, `:3322` "if(echo_autostart) {", which replaces every participant's mix. |
| SC-49 | 4.3 | 85 | If the mixer cannot deliver audio a listener should be receiving, it says so on the data channel | MIXER | | UNMET | Criterion: nothing is emitted on the data channel. `git -C D:/legion-voice-mixer grep -n -i -E '"diag"\|undeliver\|cannot deliver\|no_audio\|media_dead\|not receiving\|downstream' -- src` → 0 matches. The data channel carries only presence (`MIXER src/janus_slvoice.c:829` "gateway->relay_data(s->handle, &data);") and the power/VAD batch (`:2626`, `:2644`). The no-media reap writes only a server log line, `:3580` "reaped from room %"PRIu64": no media %us after join". |
| SC-50 | 4.3 | 85 | The viewer can surface the mixer's report that audio cannot be delivered | VIEWER | | BLOCKED | Surfacing a report in the viewer is a viewer change. Constraint: stock SL/Firestorm viewer compatibility; viewer changes only when unavoidable, and then fork-only. |
| SC-51 | 4.3 | 85 | The system must never render the appearance of working voice over a dead media path | BOTH | | PARTIAL | **Met:** an inactive source's indicator is zeroed, `MIXER src/janus_slvoice.c:2847` "g_atomic_int_set(&s->power_p, 0);", and a downed duplicate handle cannot override a live one, `:2605` "(comment) a downed handle never overwrites a live one". **Not met:** power/VAD come from the *source's* inbound decode, `:2886` "g_atomic_int_set(&s->vad, rms > SLV_VAD_RMS ? 1 : 0);". The batch goes to every listener whose data channel is open, `:2620` "if(!g_atomic_int_get(&p->dc_open))", with no check of that listener's outbound audio. A listener whose downstream audio is dead while the data channel lives still sees speaking indicators animate. |
| SC-52 | 4.4 | 89 | Structured per-session event log (join, ICE transitions, first media, stalls, teardown cause) | BOTH | | UNMET | `git -C D:/legion-voice-mixer grep -n -i -E 'notify_event\|events_is_enabled\|janus_events' -- src` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'broadcast\|eventhandler\|evh' -- docker-entrypoint.sh Dockerfile docker-compose.yml env.sample etc` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'session ?event\|event ?log\|SessionLog\|teardown ?cause\|first ?media\|ice ?transition' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. Janus core event broadcast is off by default, `MIXER vendor/janus-gateway/src/janus.c:5552` "gboolean enable_events = FALSE;", and the entrypoint adds nothing, `MIXER docker-entrypoint.sh:274` "exec "$JANUS_BIN" "$@"". |
| SC-53 | 4.4 | 89 | The session event log is retained short-term for postmortems | OPS | | UNMET | Criterion: no bounded short-term retention of a session event log. `git -C D:/legion-voice-mixer grep -n -i -E 'session ?event\|event ?log\|teardown ?cause\|retention\|postmortem\|ring ?buffer' -- src docker-entrypoint.sh docker-compose.yml etc` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'logging\|max-size\|max-file' -- docker-compose.yml` → 0 matches. |
| SC-54 | 4.5 | 93 | Fleet observability (activates at scale): per-mixer health, session counts, mix-deadline overrun rates, relay-vs-direct path ratios | BOTH | | PARTIAL | Criterion applied per metric. **Not met:** per-mixer health and relay-vs-direct path ratios; overrun rates and mixer-wide session counts are only partly present. **Mix-deadline overruns — partly:** a cumulative count per room, not a rate and not mixer-wide, `MIXER src/janus_slvoice.c:3217` "room->tick_overruns++;", exposed at `:1463` "json_object_set_new(hist, "overruns",". **Session counts — partly:** per room, inside each session's query only, `:1455` "json_object_set_new(info, "room_participants",". **Per-mixer health — not found:** `git -C D:/legion-voice-mixer grep -n -i -E 'health\|session_count\|num_sessions' -- src` → 1 match, an unrelated comment at `:1713`. The admin handler accepts only `peer_ctl_batch`, `:1958` "json_string("unknown_request")". **Relay-vs-direct ratio — not found:** `git -C D:/legion-voice-mixer grep -n -i -E 'srflx\|prflx\|relay_ratio\|candidate_type\|selected_pair\|relay.vs\|turn_used\|"relay"' -- src docker-entrypoint.sh etc` → 0 matches; the same pattern plus `\|overrun` over `git -C D:/tranq-ais … -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-55 | 4.5 | 93 | On a single-mixer grid fleet observability is one status page; nothing else changes | OPS | | UNMET | No status page in either tree. `git -C D:/legion-voice-mixer grep -n -i -E 'text/html\|<html\|status page\|dashboard' -- src etc connectors docker-entrypoint.sh docker-compose.yml Dockerfile` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'text/html\|<html\|status page\|dashboard' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-56 | 5 | 101 | Internal pipeline at 48 kHz float end-to-end; no resampling after ingest | MIXER | | MET | **Decode:** at 48 kHz into float, `MIXER src/janus_slvoice.c:2692` "s->dec = opus_decoder_create(SLV_RATE, SLV_CHANNELS, &err);" (`:130` "#define SLV_RATE            48000"), then `:2861` "opus_decode_float(s->dec, pl, pn, s->decbuf, SLV_DECODE_MAX, 0)". **Mix:** in float, `MIXER src/mixer/mix.c:43` "out[i]     += src[i]     * gL;". **Encode:** float, at the same rate, `MIXER src/janus_slvoice.c:2699` "s->enc = opus_encoder_create(SLV_RATE, SLV_CHANNELS, OPUS_APPLICATION_VOIP, &err);" → `:2926` "opus_encode_float(s->enc, frame, samples, s->outbuf + 12,". **No resampler:** `git -C D:/legion-voice-mixer grep -n -i -E 'resampl\|src_process\|swr_' -- src` → 0 matches. |
| SC-57 | 5 | 102 | No server-side noise suppression or AGC, ever | MIXER | | MET | **The chain between decode and encode** is decode `MIXER src/janus_slvoice.c:2861`, then sum `:3175` "slv_mix_nminus1_stereo(frame, SLV_FRAME_TOTAL, srcbuf, audible,", clamp `:3177` "slv_mix_clamp(frame, SLV_FRAME_TOTAL);", and encode `:3203` "janus_slvoice_encode_relay(s, frame, SLV_FRAME_SAMPLES, silence);". The clamp is a hard clip at ±1: `MIXER src/mixer/mix.c:79` "if(buf[i] > 1.0f)". **The only gains** are the viewer's per-source volume, `MIXER src/janus_slvoice.c:3117` "gains[j] = s->peer_ctl[k].gain;", and the distance falloff, `:3154` "gains[j] *= (float)pow(t, slv_spatial.falloff_exp);". **No NS or AGC code:** `git -C D:/legion-voice-mixer grep -n -i -E 'noise.?supp\|denois\|rnnoise\|speex\|preprocess\|\bagc\b\|auto(matic)?.?gain\|compressor\|webrtc_apm\|audio_processing\|OPUS_SET_GAIN' -- src` → 0 matches. **Not checkable:** "ever" commits to the future. |
| SC-58 | 5 | 103 | Talker ingest ≥ 32 kbps Opus | MIXER | | UNMET | Criterion: the configured or negotiated Opus ingest bitrate floor. **Negotiated:** none. The answer's Opus fmtp has no bitrate parameter, `MIXER src/janus_slvoice.c:2043` "minptime=10;useinbandfec=1;stereo=1;sprop-stereo=1;maxplaybackrate=48000", so the viewer's encoder picks the talker bitrate. **Configured:** none. The only bitrate setting is on the listener-mix encoder, `:2709` "opus_encoder_ctl(s->enc, OPUS_SET_BITRATE(SLV_OPUS_BITRATE));". `git -C D:/legion-voice-mixer grep -n -i -E 'maxaveragebitrate\|b=AS\|b=TIAS\|OPUS_GET_BITRATE\|min.?bitrate\|x-google' -- src etc docker-entrypoint.sh env.sample docker-compose.yml` → 0 matches; the same pattern over `git -C D:/tranq-ais … -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. **Measured:** the mixer does not measure inbound bitrate either; it counts packets, `:3349` "session->rtp_in_count++;". The bitrate stock viewers actually send was not measured in this pass. |
| SC-59 | 5 | 103 | Listener mix 64–96 kbps stereo Opus | MIXER | | MET | **Stereo:** the encoder is stereo, `MIXER src/janus_slvoice.c:2699` "opus_encoder_create(SLV_RATE, SLV_CHANNELS, OPUS_APPLICATION_VOIP, &err);" (`:131` "#define SLV_CHANNELS        2"), and stereo is negotiated, `:2043` "stereo=1;sprop-stereo=1". **Bitrate:** set at `:2709` "OPUS_SET_BITRATE(SLV_OPUS_BITRATE)" from `:177` "#define SLV_OPUS_BITRATE    64000". That is the bottom of the range. It can be changed only at build time: `:176` "#ifndef SLV_OPUS_BITRATE". |
| SC-60 | 5 | 103 | 20 ms frames | MIXER | | MET | **Frame size:** the listener mix is encoded one 20 ms frame per tick, `MIXER src/janus_slvoice.c:132` "#define SLV_TICK_MS         20" and `:133` "#define SLV_FRAME_SAMPLES   ((SLV_RATE/1000)*SLV_TICK_MS)", encoded at `:3203` "janus_slvoice_encode_relay(s, frame, SLV_FRAME_SAMPLES, silence);". **Answer:** it pins the frame with `:2048` "janus_sdp_attribute_create("ptime", "%d", 20)" and `:2049` "janus_sdp_attribute_create("maxptime", "%d", 20)". **Observation (no effect on this verdict):** the same fmtp still offers `minptime=10` (`:2043`), and nothing enforces 20 ms on ingest. A shorter decoded frame sets only `:2875` "s->dec_samples = samples;", but the mix always reads a full `SLV_FRAME_TOTAL` from `decbuf` (`:3175`). |
| SC-61 | 5 | 104 | DTX/VAD-gated decode: silent talkers cost nothing before the RTP layer | MIXER | | PARTIAL | **Met — a talker that stops sending is not decoded:** it stays in the active set only within the release hold of its last RTP, `MIXER src/janus_slvoice.c:2841` "gboolean act = (s->last_rtp_us != 0 &&". Once inactive it skips decode: `:2845` "if(!s->media_ready \|\| s->dec == NULL \|\| !act) {". **Not met (1) — the gate is packet arrival, not VAD.** The VAD flag comes from decoded energy, `:2886` "g_atomic_int_set(&s->vad, rms > SLV_VAD_RMS ? 1 : 0);", and is read only for the power batch, `:2580` "gboolean pvad = g_atomic_int_get(&p->vad) ? TRUE : FALSE;". `git -C D:/legion-voice-mixer grep -n -E '(->\|\.)vad\b' -- src` → 7 matches: `:2580` and six writes. A talker that keeps sending non-DTX silence is decoded every tick. **Not met (2) — the mixer does not request DTX:** `git -C D:/legion-voice-mixer grep -n -i -E 'usedtx' -- src` → 0 matches, and the answer fmtp (`:2043`) has no `usedtx`. Whether a silent talker stops sending is left to the viewer. |
| SC-62 | 5 | 104 | A 100–200 ms release hold prevents word-onset chopping from DTX hangover | MIXER | | MET | `MIXER src/janus_slvoice.c:151` "#define SLV_VAD_RELEASE_MS  150", applied at `:2842` "(now - s->last_rtp_us) <= (gint64)SLV_VAD_RELEASE_MS * 1000);". The hold is measured from the last RTP packet. |
| SC-63 | 5 | 105 | Encode-skip: listeners whose entire audible set is silent receive DTX and cost near-zero encode | MIXER | | MET | **Silence test:** a listener's frame is marked silent if no source was summed or the mix is at or below the floor, `MIXER src/janus_slvoice.c:3200` "gboolean silence = (summed == 0) \|\| (out_rms <= SLV_VAD_RMS);". **Skip:** the encode is then skipped, `:2922` "if(silence \|\| s->enc == NULL \|\| s->outbuf == NULL) {", returning before `:2926` "opus_encode_float(". A frame that Opus DTX reduces to no data is not sent either: `:2932` "if(enc_len <= 2) {". **Notes:** (1) A skipped tick sends no packet at all; only the RTP timestamp advances, `:2921` "s->out_ts += (uint32_t)samples;". (2) The test uses the mixed output's RMS against `:147` "#define SLV_VAD_RMS         0.02f". A mix that is audible but at or below that level is also skipped, for example a single quiet talker attenuated by distance. |
| SC-64 | 5 | 105 | Encode cost scales with audible listeners, not connected listeners | MIXER | | MET | The encode runs only for a listener whose mix is not silent (`MIXER src/janus_slvoice.c:3200`, `:2922`). A connected listener with nothing audible never reaches `:2926` "opus_encode_float(". **Note:** the mix build before the encode is not skipped. Every listener with media up runs the per-source loop, `:3095` "for(int j = 0; j < count; j++) {", and the sum, `:3175`. Mixing cost therefore still scales with connected listeners times room size. |
| SC-65 | 5 | 106 | Degradation ladder under CPU pressure, in order: frame size up (20→30 ms) → distance tiers widened → far-field talkers shed from mixes → admission refusal (last resort) | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** DSP deferred by decision; the mixer renders distance falloff and constant-power pan (SC-5). Pass 1b's finding, kept for the record: No step of the ladder exists. **Frame size:** a compile-time constant, `MIXER src/janus_slvoice.c:132` "#define SLV_TICK_MS         20". **Overruns:** only counted and logged, `:3217` "room->tick_overruns++;" and `:3221` "took %.1fms (>%.0fms deadline)". **Admission refusal:** only a fixed participant cap, not a CPU signal, `:2262` "if(room_pop >= SLV_MAX_MIX) {". **Searches:** `git -C D:/legion-voice-mixer grep -n -i -E 'degrad\|ladder\|cpu.?pressure\|load.?shed\|\bshed\|getrusage\|CPUTIME\|saturat\|far.?field' -- src` → 0 matches. No distance tiers to widen: `git -C D:/legion-voice-mixer grep -n -i -E 'lowpass\|low.?pass\|biquad\|one.?pole\|ambien\|\btier' -- src` → 0 matches. |
| SC-66 | 5 | 106 | Per-region tick isolation so one saturated region cannot starve its neighbors | MIXER | | PARTIAL | **Met — each room ticks on its own thread and lock:** `MIXER src/janus_slvoice.c:3258` "room->ticker = g_thread_try_new(tname, janus_slvoice_room_ticker, room, &err);". The tick locks only its own room, `:3028` "janus_mutex_lock(&room->mutex);", and that room's sessions, `:3059` "janus_mutex_lock(&s->mutex);". A thread that falls behind resets its own schedule instead of bursting: `:3243` "next = now;". **Not met — nothing bounds a saturated room's CPU:** there is no priority, affinity or CPU budget, so busy rooms compete for cores under the OS scheduler. `git -C D:/legion-voice-mixer grep -n -i -E 'sched_\|setpriority\|\bnice\b\|affinity\|cpuset\|g_thread_set_priority' -- src` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'cpus\|cpu_\|cpuset\|nice' -- docker-compose.yml docker-entrypoint.sh Dockerfile` → 0 matches. |
| SC-67 | 6 | 112 | No global spatial index | MIXER | | MET | Each listener measures every source in its own room directly, `MIXER src/janus_slvoice.c:3095` "for(int j = 0; j < count; j++) {" → `:3143` "double dcull = slv_vec3_mag(slv_vec3_sub(s->snap_lp, sess[j]->snap_sp));". The room's participant table is the only grouping: `:3037` "g_hash_table_iter_init(&it, room->participants);". `git -C D:/legion-voice-mixer grep -n -i -E 'kd.?tree\|octree\|quadtree\|spatial.?(index\|hash\|grid)\|bvh\|r.?tree' -- src` → 0 matches. |
| SC-68 | 6 | 112 | Culling and per-listener rendering with load-adaptive aggregation | MIXER | | MET | **Load-adaptive aggregation DESCOPED 2026-09-14 (DSP deferred by decision; a decision, not a finding).** The verdict covers the rest of the row, culling and per-listener rendering, which pass 1b found met. Pass 1b's evidence, kept for the record: **Met — culling and per-listener rendering:** each listener runs its own distance cull, `MIXER src/janus_slvoice.c:3144` "if(janus_slvoice_distance_cull_locked(s, disp, dcull, room->tick_seq)) {", then attenuation, `:3154` "gains[j] *= (float)pow(t, slv_spatial.falloff_exp);", and pan, `:3162` "double az = slv_azimuth(s->snap_lp, s->snap_lh, sess[j]->snap_sp);", summed at `:3175`. **Not met — load-adaptive aggregation:** every source that survives the cull is rendered individually at every occupancy. `git -C D:/legion-voice-mixer grep -n -i -E 'binning\|azimuth.?bin\|n_bins\|bin_count\|aggregat\|talker.?count' -- src` → 0 matches; `git -C D:/legion-voice-mixer grep -n -i -E 'degrad\|ladder\|cpu.?pressure\|load.?shed\|\bshed\|getrusage\|CPUTIME\|saturat\|far.?field' -- src` → 0 matches. |
| SC-69 | 6 | 114 | Cull first: distance, VAD, inbound level | MIXER | | PARTIAL | **Met — distance, culled first:** the distance cull, `MIXER src/janus_slvoice.c:3144`, runs after exclusion and mute (`:3132` "if(mutes[j])") and before attenuation (`:3146` "} else if(dcull > slv_spatial.ref_dist) {") and pan (`:3161` "if(!mutes[j]) {"). **Partly met — VAD:** the activity cull is RTP arrival within the release hold, `:2841`–`:2842`, and a source with no decoded frame is left out of the mix, `:3061` "audible[i] = (s->dec_samples > 0) ? 1 : 0;". The VAD flag itself (`:2886`) gates nothing in the mix; its only read is the power batch at `:2580`. `git -C D:/legion-voice-mixer grep -n -E '(->\|\.)vad\b' -- src` → 7 matches: `:2580` and six writes. **Not met — inbound level:** the decoded level is kept for diagnostics only, `:2882` "s->last_rms = rms;", reported at `:1394` "json_object_set_new(info, "last_rms", json_real(session->last_rms));". `git -C D:/legion-voice-mixer grep -n -i -E 'last_rms\|inbound.?level\|level.?cull' -- src` → 6 matches, none in the cull: the field and its comment (`:495`, `:498`), the query output (`:1394`, `:1395`), a reset (`:2731`) and the store (`:2882`). |
| SC-70 | 6 | 115 | Direct per-source HRTF below a talker-count threshold | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** DSP deferred by decision; the mixer renders distance falloff and constant-power pan (SC-5). Pass 1b's finding, kept for the record: **No HRTF code:** `git -C D:/legion-voice-mixer grep -n -i -E 'hrir\|sofa\|convol\|impulse.?resp' -- src` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'hrtf\|hrir\|\bitd\b' -- src` → 3 matches, each stating HRTF is absent: `MIXER src/janus_slvoice.c:31` "(comment) No HRTF or ITD.", the plugin description string at `:86` "no HRTF or ITD", and `MIXER src/mixer/pan.h:27` "(comment) front/back is what the deferred HRTF/ITD are for". **What exists instead:** each source gets a constant-power amplitude pan, `MIXER src/mixer/pan.h:53` "double p = (sin(azimuth) + 1.0) / 2.0;", applied at every talker count (`MIXER src/janus_slvoice.c:3164` "slv_pan(az, &pl, &pr);"). |
| SC-71 | 6 | 115 | Azimuth binning engages only above the talker-count threshold | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** DSP deferred by decision; the mixer renders distance falloff and constant-power pan (SC-5). Pass 1b's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'binning\|azimuth.?bin\|n_bins\|bin_count\|aggregat\|talker.?count' -- src` → 0 matches. The tick has no branch on talker count. The per-listener loop, `MIXER src/janus_slvoice.c:3095` "for(int j = 0; j < count; j++) {", runs the same cull, attenuation and pan (`:3143`–`:3167`) for every source. |
| SC-72 | 6 | 115 | Crossfades over several 10 ms frames at bin crossings, pre-staged from position derivatives | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** DSP deferred by decision; the mixer renders distance falloff and constant-power pan (SC-5). Pass 1b's finding, kept for the record: Criterion: are there crossfades at bin crossings at all? No. `git -C D:/legion-voice-mixer grep -n -i -E 'crossfade\|cross.?fade\|xfade\|fade' -- src` → 0 matches. There is no binning either: `git -C D:/legion-voice-mixer grep -n -i -E 'binning\|azimuth.?bin\|n_bins\|bin_count\|aggregat\|talker.?count' -- src` → 0 matches. Per-source gains are recomputed each tick and held constant across the 20 ms frame, with no ramp between ticks: `MIXER src/mixer/mix.c:43` "out[i]     += src[i]     * gL;". |
| SC-73 | 6 | 115 | At low occupancy the binning path and its artifacts don't run | MIXER | | MET | **Met vacuously:** no binning path exists at any occupancy, so none runs at low occupancy. `git -C D:/legion-voice-mixer grep -n -i -E 'binning\|azimuth.?bin\|n_bins\|bin_count\|aggregat\|talker.?count' -- src` → 0 matches. Every tick renders each source directly: `MIXER src/janus_slvoice.c:3162` "slv_azimuth(s->snap_lp, s->snap_lh, sess[j]->snap_sp)" → `:3164` "slv_pan(az, &pl, &pr);" → `:3175` "slv_mix_nminus1_stereo(". **Not implemented:** the threshold design this claim describes (SC-71). |
| SC-74 | 6 | 116 | Distance tiers: full HRTF with ITD near; amplitude panning + lowpass mid; single mono ambience sum far | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** DSP deferred by decision; the mixer renders distance falloff and constant-power pan (SC-5). Pass 1b's finding, kept for the record: **No distance tiers:** `git -C D:/legion-voice-mixer grep -n -i -E 'lowpass\|low.?pass\|biquad\|one.?pole\|ambien\|\btier' -- src` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'hrir\|sofa\|convol\|impulse.?resp' -- src` → 0 matches. **What exists** is one treatment at every distance: full volume inside the reference radius, then falloff to the cutoff (`MIXER src/janus_slvoice.c:3146` "} else if(dcull > slv_spatial.ref_dist) {" → `:3154`), and an amplitude pan, `:3164` "slv_pan(az, &pl, &pr);". **Far field:** beyond the cutoff a source is dropped, not summed into a mono ambience bed: `:3145` "mutes[j] = 1;". |
| SC-75 | 6 | 117 | Dirty-flagged coefficient recompute: stationary listener + stable talker set skips HRIR selection/interpolation setup per frame | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** DSP deferred by decision; the mixer renders distance falloff and constant-power pan (SC-5). Pass 1b's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'dirty\|recompute\|coeff' -- src` → 0 matches. There is no HRIR selection to skip. The coefficients that do exist are recomputed for every pair on every tick, whether anything moved or not: distance `MIXER src/janus_slvoice.c:3143` "double dcull = slv_vec3_mag(", falloff `:3154` "gains[j] *= (float)pow(t, slv_spatial.falloff_exp);", pan `:3162` "double az = slv_azimuth(". |
| SC-76 | 6 | 118 | Listener orientation honored from the SLData `lh` quaternion | MIXER | | MET | **Parsed** from SLData, `MIXER src/sldata.c:146` "if(slv_get_vecN(json_object_get(root, "lh"), 4, xyzw, vec)) {". **Snapshotted** each tick, `MIXER src/janus_slvoice.c:2973` "s->snap_lh = (f & SLV_FIELD_LH) ? s->last_data.lh", defaulting to identity when absent, `:2974` "(slv_quat){ 0.0, 0.0, 0.0, 1.0 };". **Used** for the source's azimuth in the listener's frame, `:3162` "slv_azimuth(s->snap_lp, s->snap_lh, sess[j]->snap_sp)", where the quaternion sets the forward vector: `MIXER src/mixer/azimuth.h:57` "double fy = 2.0 * (q.x * q.y + q.w * q.z);". |
| SC-77 | 6 | 119 | Per-(listener, source) crossfade state kept in flat preallocated arrays sized at session setup; no per-transition allocation | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** DSP deferred by decision; the mixer renders distance falloff and constant-power pan (SC-5). Pass 1b's finding, kept for the record: No crossfade state exists. `git -C D:/legion-voice-mixer grep -n -i -E 'crossfade\|cross.?fade\|xfade\|fade' -- src` → 0 matches. **Note:** the one per-(listener, source) state that does exist, the distance-cull hysteresis, follows the pattern this claim describes. It is a fixed array in the session, `MIXER src/janus_slvoice.c:445` "slv_cull_hyst cull_hyst[SLV_MAX_MIX];", and a transition reuses a slot without allocating, `:3012` "memset(slot, 0, sizeof(*slot));". |
| SC-78 | 7.1 | 127 | Restores the Vivox-era "hear from camera" behavior without reopening the eavesdropping hole | BOTH | | MET | **Camera hearing:** the listener position is the viewer's camera `lp`, `MIXER src/janus_slvoice.c:2972` "s->snap_lp = s->last_data.lp;". The server clamps it to a leash around the avatar: `:2979` "if(d > slv_spatial.leash_dist)" → `:2980` "s->snap_lp = slv_vec3_madd(s->snap_sp, slv_spatial.leash_dist / d, off);". **`lp` grants no access.** It feeds only the distance cull, attenuation and pan (`:3143`, `:3162`). Those run after the sim's exclusion and moderation mute, `:3125` "if(slv_roster_excludes(s->excluded, disp))" and `:3132` "if(mutes[j])", so `lp` can only remove audio. The exclusion is built from the avatar's position, not the camera: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/FeederWorldFromScene.cs:66` "sp.UUID, sp.IsChildAgent, sp.AbsolutePosition, sp.currentParcelUUID, sp.IsViewerUIGod));". A root agent's parcel is also taken from its position: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:673` "GetLandObject(sp.AbsolutePosition.X, sp.AbsolutePosition.Y)". **Note:** the exclusion exists only while the feeder runs, and it is off by default: `:116` "GetBoolean("VisibilityFeederEnabled", false)". The child-agent parcel hint is recorded under SC-80. |
| SC-79 | 7.1 | 129 | Attenuation and HRTF may follow client-reported listener position within an estate-configurable leash radius of the avatar | BOTH | | PARTIAL | **Met — attenuation and pan follow the leashed camera position.** The leash is at `MIXER src/janus_slvoice.c:2980`. The cull and falloff distance is measured from `snap_lp`, `:3143` "slv_vec3_mag(slv_vec3_sub(s->snap_lp, sess[j]->snap_sp))" and `:3154` "gains[j] *= (float)pow(t, slv_spatial.falloff_exp);", and so is the pan, `:3162` "slv_azimuth(s->snap_lp, s->snap_lh, sess[j]->snap_sp)". There is no HRTF (SC-70), which this claim's "may" does not require. **Not met — estate-configurable.** The radius is one process-wide mixer setting: `:1080` "janus_slvoice_cfg_double(config, general, "spatial_leash_distance_m",     &leash_m);", template `MIXER etc/janus/janus.plugin.slvoice.jcfg:30` "#spatial_leash_distance_m     = 50.0". `git -C D:/tranq-ais grep -n -i -E 'leash' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'leash' -- src etc docker-entrypoint.sh env.sample` → 22 matches, none in room create or the admin API: the jcfg template line, the `[general]` parse (`MIXER src/janus_slvoice.c:1074`–`:1110`), the default (`:196`, `:209`, `:216`), the snapshot (`:2975`–`:2980`, `:3063`) and comments. |
| SC-80 | 7.1 | 130 | Parcel/estate audibility is always computed from the avatar's authoritative position; camming never grants audio access the avatar's position wouldn't | SIM | | PARTIAL | **Met — visibility and root-agent provisioning use the sim's position.** The feeder reads it, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/FeederWorldFromScene.cs:66` "sp.AbsolutePosition, sp.currentParcelUUID". The parcel comes from the cached parcel, `SIM Addons/os-webrtc-janus/Visibility/VisibilityMatrix.cs:99` "if (!a.IsChild && a.CurrentParcelUUID != UUID.Zero)" → `:100`, or from position, `:101` "return world.GetParcelAt(a.Position);". Root provisioning uses position: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:673` "GetLandObject(sp.AbsolutePosition.X, sp.AbsolutePosition.Y)". The camera feeds neither; the mixer applies `lp` only after exclusion (`MIXER src/janus_slvoice.c:3132` "if(mutes[j])"). **Not met — child agents.** For a neighbour-region provision, the viewer's `parcel_local_id` picks the parcel: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:675` "if (res.Source == ParcelSource.ClientHint)" → `:676` "parcel = scene.LandChannel.GetLandObject(res.LocalId.Value);". That parcel drives the voice-allowed check, `:706` "(land.Flags & (uint)ParcelFlags.AllowVoiceChat) == 0", and the ban/restrict check, `:732` "parcel.IsRestrictedFromLand(agentID)". The id is also forwarded unchanged into the room, `:724` "(comment) the client's parcel_local_id is forwarded unchanged". Ledger O-77. |
| SC-81 | 7.2 | 134 | Performer mode: a per-avatar, estate-grantable flag | SIM | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** performer mode not built. Pass 1c's finding, kept for the record: `git -C D:/tranq-ais grep -n -i -E 'performer' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'performer' -- Source OpenSim ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'performer\|high.?bitrate\|stereo.?ingest' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample` → 0 matches. |
| SC-82 | 7.2 | 134 | Performer mode: high-bitrate stereo ingest | BOTH | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** performer mode not built. Pass 1c's finding, kept for the record: **No performer mode:** `git -C D:/legion-voice-mixer grep -n -i -E 'performer\|high.?bitrate\|stereo.?ingest' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample` → 0 matches; `git -C D:/tranq-ais grep -n -i -E 'performer' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. **No per-stream ingest bitrate:** `git -C D:/legion-voice-mixer grep -n -i -E 'maxaveragebitrate\|b=AS\|b=TIAS\|OPUS_GET_BITRATE\|min.?bitrate\|x-google' -- src etc docker-entrypoint.sh env.sample docker-compose.yml` → 0 matches; the same pattern over `git -C D:/tranq-ais … -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. Every answer carries the same Opus fmtp, `MIXER src/janus_slvoice.c:2043` "minptime=10;useinbandfec=1;stereo=1;sprop-stereo=1;maxplaybackrate=48000". Stereo is accepted from every talker, not granted per performer. |
| SC-83 | 7.2 | 134 | Performer mode: client signaled to disable NS/AGC for that stream | VIEWER | BLOCKED-BY-CONSTRAINT | BLOCKED | Pass-0b ruling: signalling the client to disable NS/AGC needs a viewer change. Constraint: stock SL/Firestorm viewer compatibility; viewer changes only when unavoidable, and then fork-only. |
| SC-84 | 7.2 | 134 | Performer mode: exempt from far-tier degradation | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** performer mode not built. Pass 1c's finding, kept for the record: **No performer mode:** `git -C D:/legion-voice-mixer grep -n -i -E 'performer\|high.?bitrate\|stereo.?ingest' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample` → 0 matches. **No far-tier degradation to be exempt from:** `git -C D:/legion-voice-mixer grep -n -i -E 'degrad\|ladder\|cpu.?pressure\|load.?shed\|\bshed\|getrusage\|CPUTIME\|saturat\|far.?field' -- src` → 0 matches. The distance cull treats every source alike: `MIXER src/janus_slvoice.c:3144` "if(janus_slvoice_distance_cull_locked(s, disp, dcull, room->tick_seq)) {". |
| SC-85 | 7.3 | 138 | Per-listener visibility matrix over a shared estate/region mix: avatars can be hidden from each other based on parcel access, enforced in the mix (§3.4) | BOTH | AMENDED | MET | Judged against the amended mechanism: per-parcel rooms with an estate-room fallback, per-listener, enforced in the mix. **Per-parcel rooms:** the "local" room number hashes the parcel id, `SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:251` "hasher.Add(pParcelLocalID);". For a root agent that id is the server's: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:722` "map["parcel_local_id"] = OSD.FromInteger(land.LocalID);". **Estate fallback:** an estate-channel parcel drops the id, `:716` "map.Remove("parcel_local_id"); // estate channel". The service then uses the estate room: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:385` "pRequest.TryGetInt("parcel_local_id", out int pli) ? pli : JanusAudioBridge.REGION_ROOM_ID;". **Per listener, per room:** the sink addresses each listener's recorded room and falls back to the estate room, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/JanusPeerCtlBatchSink.cs:181` "PeerCtlBatchPartitioner.Partition(excl, RoomOf, _fallbackRoom);". The room is stamped per request at `:220` "request["room"] = new OSDInteger(roomKey);". **Rule:** hiding is by parcel access, `SIM Addons/os-webrtc-janus/Visibility/VisibilityRules.cs:41` "if (sourceParcel.ExcludesByBan(listener.Id) \|\| listenerParcel.ExcludesByBan(source.Id))". **In the mix:** `MIXER src/janus_slvoice.c:3125` "if(slv_roster_excludes(s->excluded, disp))" → `:3126` "mutes[j] = 1;". **Note:** this is inactive on a fresh deployment. The feeder and emitter both default to false: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:116` "GetBoolean("VisibilityFeederEnabled", false)" and `:118` "GetBoolean("VisibilityEmitEnabled", false)" (ledger O-78). |
| SC-86 | 7.3 | 138 | Participant lists, voice dots, and power levels are driven by the same SLData visibility set, so a hidden avatar is hidden in both audio and UI consistently, from one source of truth | MIXER | | MET | One per-listener set, `excluded`, drives all three. **Audio:** `MIXER src/janus_slvoice.c:3125` "if(slv_roster_excludes(s->excluded, disp))". **Participant list (join presence):** `:873` "if(slv_roster_excludes(p->excluded, who))" and `:929` "if(slv_roster_excludes(listener->excluded, p->display))". **Voice dots and power:** each listener's batch omits the sources it excludes, `:2634` "if(slv_roster_excludes(p->excluded, bk))". **Source of truth:** the set comes from one predicate in the sim, `SIM Addons/os-webrtc-janus/Visibility/VisibilityRules.cs:15` "public static bool IsExcluded(". Moderation mute is deliberately a separate channel that keeps the source listed (`:30`–`:35`, comment). |
| SC-87 | 7.3 | 138 | A dot never lights with no audio behind it | MIXER | | PARTIAL | **Addressed by mixer `4fbfaf4` (V-3, ledger O-84); not re-verdicted.** Each listener's power batch now reports `{p:0, v:false}` for sources moderation-muted, personally muted or distance-culled for it; harness S11 verifies the moderation-mute case. The evidence below is pass 1c's, for a later pass to re-check. **Met — excluded and silent sources:** an excluded source's dot is omitted, `MIXER src/janus_slvoice.c:2634` "if(slv_roster_excludes(p->excluded, bk))". A source with no recent RTP has its power zeroed: `:2847` "g_atomic_int_set(&s->power_p, 0);". **Not met.** Power is the source's own decoded level, `:2885` "g_atomic_int_set(&s->power_p, p);". It goes to every listener that does not exclude the source; `:2634` is the only per-listener filter in the send loop (`:2617`–`:2649`). A source the listener gets no audio from can therefore still light its dot, for example one distance-culled for that listener (`:3144` → `:3145` "mutes[j] = 1;") or one moderation-muted (`:3110` "g_hash_table_contains(s->mod_muted, disp)" → `:3111`). |
| SC-88 | 7.3 | 138 | Parcel privacy is enforced in the mix rather than as connection topology, removing SL's parcel-boundary reconnection stutter | BOTH | | PARTIAL | Tested by what decides a participant's room when the avatar crosses a parcel boundary. **The room depends on the parcel.** A "local" provision chooses its room at `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:408` "viewerSession.Room = await viewerSession.AudioBridge.SelectRoom(pSceneID.ToString(),", using the parcel id read at `:385`. The room number hashes that id: `SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:251` "hasher.Add(pParcelLocalID);". The region module sets the id from the avatar's parcel, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:722` "map["parcel_local_id"] = OSD.FromInteger(land.LocalID);", unless the parcel uses the estate channel: `:679` "(land.Flags & (uint)ParcelFlags.UseEstateVoiceChan) != 0" → `:716` "map.Remove("parcel_local_id"); // estate channel". **Nothing moves a participant on a crossing.** The sim's only parcel-entry hook marks the visibility matrix dirty: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceVisibilityService.cs:219` "private void OnAvatarEnteringNewParcel(ScenePresence sp, int localLandID, UUID regionID)" → `:221` "m_feeder.OnAvatarEnteringNewParcel();" → `SIM Addons/os-webrtc-janus/Visibility/VoiceStateFeeder.cs:64` "public void OnAvatarEnteringNewParcel() => Invalidate();". The mixer has no room-change request: `git -C D:/legion-voice-mixer grep -n -i -E 'changeroom\|change_room\|switch_room\|"move"' -- src` → 0 matches. **Met — crossing between two estate-channel parcels.** Both resolve to the estate room (`SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:385`, default `REGION_ROOM_ID`). The participant stays in one room, and only the mix changes, through the exclusion set rebuilt after the crossing (`MIXER src/janus_slvoice.c:3125`). **Not met — crossing into, out of, or between parcels with their own voice channel.** The parcel id changes, so the room number changes (`SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:251`). Hearing the new parcel takes a new provision and a join into another room: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:408`, then `:419` "var joinResult = await viewerSession.Room.JoinRoom(viewerSession)". That is connection topology, the behaviour this claim says was removed. If no new provision arrives, the participant stays in the old parcel's room, because neither the sim nor the mixer moves it. **Not checked:** whether a stock viewer re-provisions on the crossing, and how long the switch stalls. That needs the viewer's parcel-change logic or a live crossing between two parcels with their own channels. |
| SC-89 | 7.4 | 142 | Voice morphing: per-talker pitch/formant shifting applied at decode, before spatial encode | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** voice morphing not built. Pass 1c's finding, kept for the record: **Searches establishing that no voice morphing exists (they serve SC-89 – SC-94):** (1) `git -C D:/legion-voice-mixer grep -n -i -E 'morph\|pitch.?shift\|formant\|psola\|vocoder\|voice.?font\|soundtouch\|rubber.?band' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample Dockerfile Makefile` → 0 matches. (2) `git -C D:/tranq-ais grep -n -i -E 'morph\|pitch.?shift\|formant\|psola\|vocoder\|voice.?font\|soundtouch\|rubber.?band' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. (3) `git -C D:/tranq-ais grep -n -i -E 'no_?morph\|nomorph\|morph_?state' -- Source OpenSim Addons ':!*.md' ':!*.xml'` → 0 matches. (4) `git -C D:/tranq-ais grep -n -i -E 'no_?morph\|voice.?morph\|pitch.?shift\|formant\|psola\|vocoder\|voice.?font' -- Source OpenSim ':!*.md' ':!*.xml'` → 2 matches, both "formant" inside "conformant" (`SIM Source/OpenSim.Region.ClientStack.LindenCaps/ExperienceModule.cs:616`, `SIM Source/OpenSim.Server.RegionServer/AppData/OpenSim.ini.example:1042`). Without the `.xml` exclusion, the core tree also matches avatar-appearance morph targets and bake morph masks; none is voice. **This row:** a decoded frame goes straight from the decoder to the mix with no per-talker stage, `MIXER src/janus_slvoice.c:2861` "opus_decode_float(s->dec, pl, pn, s->decbuf, SLV_DECODE_MAX, 0)" → `:3062` "srcbuf[i] = s->decbuf;". |
| SC-90 | 7.4 | 142 | Morphing cost scales with morphed active talkers (typically 0–3), not listeners | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** voice morphing not built. Pass 1c's finding, kept for the record: No morphing exists: the searches listed under SC-89 → 0 matches. No per-talker processing runs between decode and mix: `MIXER src/janus_slvoice.c:2861` → `:3062` "srcbuf[i] = s->decbuf;". |
| SC-91 | 7.4 | 145 | Morphing's analysis-window latency (20–60 ms) is added to morphed talkers only | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** voice morphing not built. Pass 1c's finding, kept for the record: No morphing exists: the searches listed under SC-89 → 0 matches. Every talker gets the same fixed jitter-buffer lag and nothing else: `MIXER src/janus_slvoice.c:142` "#define SLV_JB_LAG          3". |
| SC-92 | 7.4 | 147 | Recommendation: write PSOLA in-house (~few hundred lines) to keep the distribution unencumbered (SoundTouch is LGPL, Rubber Band is GPL/commercial) | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** voice morphing not built. Pass 1c's finding, kept for the record: No in-house PSOLA and no third-party pitch library: the searches listed under SC-89 cover `psola`, `soundtouch` and `rubber.?band` → 0 matches in both trees. The licensing concern is therefore moot today; nothing is linked. |
| SC-93 | 7.4 | 148 | Estates get a `no_morphing` flag | SIM | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** voice morphing not built. Pass 1c's finding, kept for the record: `git -C D:/tranq-ais grep -n -i -E 'no_?morph\|nomorph\|morph_?state' -- Source OpenSim Addons ':!*.md' ':!*.xml'` → 0 matches (SC-89, search 3). The addon's provisioning reads the estate voice master switch, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:636` "if (!scene.RegionInfo.EstateSettings.AllowVoice)", and no morphing setting. |
| SC-94 | 7.4 | 148 | Morph state is visible to moderators | BOTH | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** voice morphing not built. Pass 1c's finding, kept for the record: No morph state exists: the searches listed under SC-89 → 0 matches, including `morph_?state`. The moderator-facing state is mute only, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceModerationStore.cs:27` "public bool MuteEveryone { get; }" and `:28` "public IReadOnlyList<UUID> MutedAgents { get; }". |
| SC-95 | 7.5 | 152 | A tap-and-inject API on the mixer (plain RTP or WebSocket), generalizing the requested post-processing hooks | MIXER | AMENDED | MET | Judged against the amended form: aiortc WebRTC peers that join a room normally. **Peer:** `MIXER connectors/common/peer.py:34` "from aiortc import RTCPeerConnection, RTCSessionDescription", created at `:47` "self._pc = RTCPeerConnection()", with an SLData channel at `:91` "channel = self._pc.createDataChannel("SLData")". **Joins like a viewer:** `:100` "{"request": "join", "room": cfg["room"], "display": cfg["display"]},". **Tap:** `MIXER connectors/recorder/recorder.py:49` "def on_audio_track(self, track) -> None:". **Inject:** `MIXER connectors/injector/injector.py:208` "def local_track(self):". The mixer admits them through its ordinary join: `MIXER src/janus_slvoice.c:2223` "json_object_get(root, "display")". |
| SC-96 | 7.5 | 154 | Taps: recording (consent-gated per §3.5) | BOTH | | PARTIAL | **Addressed in part by mixer `ae159b0` (V-3, ledger O-85); not re-verdicted.** The recorder, and the injector when recording, refuse to start without `RECORDING_OPT_IN=yes`, and the mixer marks and logs a recording tap. Per-participant consent still needs the sim (O-85). The evidence below is pass 1c's, for a later pass to re-check. **Met — the recording tap:** `MIXER connectors/recorder/recorder.py:49` "def on_audio_track(self, track) -> None:", writing WAV at `MIXER connectors/common/segments.py:119` "w = wave.open(path, "wb")". A connector registered in the sim triggers the in-region notice: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceConnector/VoiceConnectorRegistrar.cs:106` "pDisclosure?.OnAttach(pRecord);". **Not met — consent-gated.** `git -C D:/tranq-ais grep -n -i -E 'consent' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'consent' -- src connectors docker-compose.yml docker-entrypoint.sh env.sample etc` → 0 matches. The recorder takes its room from env, `MIXER connectors/common/config.py:15` ""room": os.environ.get("ROOM"),", and joins without a sim registration (`MIXER connectors/common/peer.py:100`), so the notice is not a precondition of recording. |
| SC-97 | 7.5 | 154 | Taps: transcription | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the transcription tap not built. Pass 1c's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'transcri\|whisper\|speech.?to\|\bstt\b\|\basr\b\|vosk' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'transcri' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. The only tap consumer writes WAV: `MIXER connectors/common/segments.py:119` "w = wave.open(path, "wb")". |
| SC-98 | 7.5 | 154 | Taps: future translation | MIXER | NOT-A-COMMITMENT | NOT-A-COMMITMENT | Pass-0b ruling: the spec itself says "future translation", so there is no finding to make. Recorded for completeness: `git -C D:/legion-voice-mixer grep -n -i -E 'translate\|deepl' -- src connectors` → 0 matches; `git -C D:/tranq-ais grep -n -i -E 'translate\|deepl' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-99 | 7.5 | 155 | Injectors: NPC/bot TTS voices as first-class positioned sources | BOTH | | PARTIAL | **Met — bot voices as first-class participants.** A registered connector gets a stable NPC identity, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceConnector/VoiceConnectorModule.cs:288` "UUID derivedId = ConnectorIdentity.DeriveAgentId(m_gridId, scene.RegionInfo.RegionName, record.Name);". It gets a scene NPC, `:290` "pCreateNpc: r => m_npcModule.CreateNPC(r.NpcFirstName, r.NpcLastName, r.Position, derivedId,", and an ordinary "local" provision, `:296` "new OSDMap { { "channel_type", OSD.FromString("local") } }, npcId, scene.RegionInfo.RegionID);". The injector sends audio as a participant: `MIXER connectors/injector/injector.py:208` "def local_track(self):". **Not met (1) — positioned.** The peer opens SLData but never sends on it: `git -C D:/legion-voice-mixer grep -n -E '"sp"\|"lp"\|\.send\(' -- connectors` → 0 matches. The mixer therefore has no geometry for it and leaves it in the flat, unattenuated mix: `MIXER src/janus_slvoice.c:3141` "if(!s->snap_valid \|\| !sess[j]->snap_valid)" → `:3142` "continue;". **Not met (2) — TTS.** `git -C D:/legion-voice-mixer grep -n -i -E '\btts\b\|text.?to.?speech\|espeak\|piper\|coqui\|pyttsx' -- connectors ':!*.md'` → 1 match, a docstring at `MIXER connectors/injector/source.py:3` "(docstring) the TTS engine and the live mic are just different writers". The injector reads only a WAV file or pipe: `MIXER connectors/injector/injector.py:50` ""source": os.environ.get("SOURCE", "/injector/in.wav"),". |
| SC-100 | 7.5 | 155 | Injectors: external DJ/stream sources entering spatial voice as positioned audio rather than parcel media URLs | BOTH | | PARTIAL | **Met — external audio can enter a voice room.** The injector plays a WAV file or a FIFO fed by any writer, `MIXER connectors/injector/injector.py:50` ""source": os.environ.get("SOURCE", "/injector/in.wav")," and `:66` "(docstring) FIFO: EOF means the writer closed; reopen and wait for the next writer". It joins a room like a viewer: `MIXER connectors/common/peer.py:100`. **Not met — as positioned audio.** It sends no geometry (`git -C D:/legion-voice-mixer grep -n -E '"sp"\|"lp"\|\.send\(' -- connectors` → 0 matches), so it is mixed flat: `MIXER src/janus_slvoice.c:3141` "if(!s->snap_valid \|\| !sess[j]->snap_valid)". **Note:** there is no stream client. A DJ stream must be decoded to s16 WAV by an external writer: `git -C D:/legion-voice-mixer grep -n -i -E 'http\|url\|icecast\|shoutcast\|rtmp\|ffmpeg' -- connectors/injector/injector.py connectors/injector/source.py` → 0 matches. |
| SC-101 | 7.5 | 156 | Effects sends: parcel-property environmental processing (reverb, echo zones), configurable by land settings | BOTH | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** effects sends (reverb and echo zones) not built. Pass 1c's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'reverb\|echo.?zone\|environmental\|effects?.?send' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'reverb\|echo.?zone\|environmental\|effects?.?send' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. After the sum, the mix is only clamped: `MIXER src/janus_slvoice.c:3177` "slv_mix_clamp(frame, SLV_FRAME_TOTAL);". |
| SC-102 | 7.6 | 160 | Estate-level mute/gain enforced in the mix | BOTH | | PARTIAL | **Met — mute enforced in the mix.** The sim computes it, `SIM Addons/os-webrtc-janus/Visibility/VisibilityMatrix.cs:84` "else if (VisibilityRules.IsModerationMuted(agents[si], parcels[si]))". The mixer silences the source: `MIXER src/janus_slvoice.c:3110` "if(s->mod_muted != NULL && g_hash_table_contains(s->mod_muted, disp))" → `:3111` "mutes[j] = 1;". An estate ban removes a party from the mix altogether: `SIM Addons/os-webrtc-janus/Visibility/VisibilityRules.cs:27` "if (estate.IsEstateBanned(source.Id) \|\| estate.IsEstateBanned(listener.Id))". **Not met (1) — estate level.** The mute is per parcel: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceModerationStore.cs:47` "private readonly Dictionary<UUID, ParcelModeration> m_byParcel", read against the source's parcel at `SIM Addons/os-webrtc-janus/Visibility/VisibilityRules.cs:61` "=> sourceParcel.ExcludesByModeration(source.Id);". **Not met (2) — gain.** The batch carries only exclusion and mute, `MIXER src/visbatch.c:175` "json_t *excl = json_object_get(b, "excl");" and `:182` "json_t *mute = json_object_get(b, "mute");". The only per-source gain is the viewer's own: `MIXER src/janus_slvoice.c:3117` "gains[j] = s->peer_ctl[k].gain;". `git -C D:/tranq-ais grep -n -i -E 'podium\|gain.?priority\|EstateGain\|estate.?mute\|VoiceGain' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-103 | 7.6 | 160 | "Podium" mode granting designated speakers gain priority | BOTH | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** podium mode not built. Pass 1c's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'podium\|priority\|duck' -- src connectors etc docker-compose.yml docker-entrypoint.sh env.sample` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'podium\|gain.?priority\|EstateGain\|estate.?mute\|VoiceGain' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-104 | 7.6 | 160 | Per-parcel voice zones (§7.3) as part of the moderation surface | BOTH | | MET | **Per-parcel voice zones:** a parcel with its own channel gets its own room, `SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:251` "hasher.Add(pParcelLocalID);", with the id set at `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:722` "map["parcel_local_id"] = OSD.FromInteger(land.LocalID);". **Moderated per parcel:** moderation state is keyed by parcel, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceModerationStore.cs:47` "Dictionary<UUID, ParcelModeration> m_byParcel", with a parcel-wide mute at `:51` "public void SetMuteEveryone(UUID parcelGlobalId, bool muted)". The feeder applies it per parcel, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/FeederWorldFromScene.cs:138` "&& m_moderation.IsModerated(ld.GlobalID, avatar)". The operator console exposes it: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceModerationCommands.cs:89` "AddCommand("Voice", false, "voice moderation mute",". **Note:** crossing between these zones is a room change (SC-88). |
| SC-105 | 7.6 | 160 | All moderation is mixer-enforced and therefore not bypassable by modified viewers | MIXER | | MET | Every moderation mechanism that exists is enforced in the mixer or at the sim, never in the client. **Moderation mute:** `MIXER src/janus_slvoice.c:3110` → `:3111` "mutes[j] = 1;". The viewer's own control can only add a mute: `:3114` "if(s->peer_ctl[k].muted)". **Ban and hiding:** an excluded source leaves the mix, `:3125` "if(slv_roster_excludes(s->excluded, disp))", and the roster, `:929` "if(slv_roster_excludes(listener->excluded, p->display))". **Refusal at provision:** a banned or restricted agent is refused, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:732` "if(parcel.IsRestrictedFromLand(agentID) \|\| parcel.IsBannedFromLand(agentID))". **Notes:** estate gain and podium do not exist (SC-102, SC-103). The feeder is off by default, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:116` "GetBoolean("VisibilityFeederEnabled", false)", and without it a console mute is refused rather than enforced: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceModerationCommands.cs:294` "so a mute cannot be enforced (see VisibilityFeederEnabled)". |
| SC-106 | 8 | 166 | Most deployments will be small; the small case is the default case | OPS | | MET | **Small is the default:** the shipped sim INI points the region at its own Janus for both spatial and non-spatial voice, `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini:5` "SpatialVoiceService = WebRtcJanusService.dll:WebRtcJanusService" and `:9` "NonSpatialVoiceService = WebRtcJanusService.dll:WebRtcJanusService". The compose file runs one mixer service, `MIXER docker-compose.yml:13` "janus:". The connector peers start only under a named profile, `:101` "profiles: [recorder]". **Not checkable:** "most deployments will be small" is a forecast. |
| SC-107 | 8 | 166 | A single-region standalone or small grid runs one mixer process alongside the simulator or grid services | OPS | | MET | **One mixer process:** a single compose service, `MIXER docker-compose.yml:13` "janus:". **One gateway per service:** `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:105` "_JanusServerURI = janusConfig.GetString("JanusGatewayURI", string.Empty);". **Both roles on it:** the shipped INI names the same service for both roles (`SIM Addons/os-webrtc-janus/os-webrtc-janus.ini:5`, `:9`), and identical names share one instance, `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:138` "m_nonSpatialVoiceService = m_spatialVoiceService;". |
| SC-108 | 8 | 166 | The small deployment is configured by one INI section | OPS | | UNMET | **Addressed by `3283c939f1` (fix V-1); not re-verdicted.** `PluginName` now defaults to `janus.plugin.slvoice`. The shipped INI and the example carry `PluginName`, `StunServers`, `VisibilityFeederEnabled` and `VisibilityEmitEnabled`. The addon README install section states the real requirements. The evidence below is pass 1d's, for a later pass to re-check. Counted against what working voice with the Legion mixer needs today. **Sim: two INI sections, not one.** The addon reads `[WebRtcVoice]`, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:105` "m_Config = config.Configs["WebRtcVoice"];", and `[JanusWebRtcVoice]`, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:102` "IConfig janusConfig = _Config.Configs["JanusWebRtcVoice"];". **Seven keys must be set or added** in the shipped `os-webrtc-janus.ini`: (1) `Enabled`, shipped `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini:2` "Enabled=false" and read with default false at `SIM …/WebRtcVoiceRegionModule.cs:108` "m_Config.GetBoolean("Enabled", false)". (2)–(5) `JanusGatewayURI`, `APIToken`, `JanusGatewayAdminURI`, `AdminAPIToken`, shipped as placeholders (`SIM Addons/os-webrtc-janus/os-webrtc-janus.ini:19`, `:21`, `:23`, `:25`); the tokens must equal the mixer's secrets. (6) `PluginName`, absent from the shipped INI: `git -C D:/tranq-ais grep -n -E 'StunServers\|PluginName\|VisibilityFeederEnabled' -- Addons/os-webrtc-janus/os-webrtc-janus.ini` → 0 matches. It is also absent from the example and the addon README: `git -C D:/tranq-ais grep -n -E 'PluginName\|StunServers' -- Addons/os-webrtc-janus/os-webrtc-janus.ini.example Addons/os-webrtc-janus/README.md` → 1 match, `StunServers` in the example. Its default is the stock plugin, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:112` "GetString("PluginName", "janus.plugin.audiobridge")", and the image builds Janus without disabling that plugin (`MIXER Dockerfile:40`–`:44`, the configure flags). Left unset, the sim therefore attaches to AudioBridge, not the Legion mixer. The config register records the live value as `janus.plugin.slvoice` (doc: `SIM Docs/voice/voice-config-register.md:50`). (7) `StunServers`, absent from the shipped INI (same grep) and read with an empty default at `SIM …/WebRtcVoiceRegionModule.cs:115`. Stock viewers fail without it: `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:18` "(comment) STOCK viewers REQUIRE a non-empty valid entry". **Two more** are needed for the §3 permission model: `VisibilityFeederEnabled` and `VisibilityEmitEnabled`, both default false (`SIM …/WebRtcVoiceRegionModule.cs:116`, `:118`). **Placing the INI** is a manual copy into the config folder (doc: `SIM Addons/os-webrtc-janus/README.md:72`–`:74`), which the loader reads as `inidirectory`, default `config`: `SIM Source/OpenSim.Server.RegionServer/ConfigurationLoader.cs:160`. **Mixer: three env vars with no working default,** shipped blank in `MIXER env.sample:6` "JS_PUBLIC_IP=", `:28` "JS_API_SECRET=" and `:30` "JS_ADMIN_SECRET=". An empty secret stops the container, `MIXER docker-entrypoint.sh:117` "FATAL: ${missing} empty; refusing to start" → `:118` "exit 1". No public IP means media fails, `:237` "so WebRTC MEDIA WILL FAIL for every viewer". **Manual steps:** create and edit `.env` (doc: `MIXER README.md:45`); start the container (doc: `:64` "docker compose up -d"); open the UDP media range, `MIXER env.sample:33` "(comment) open this range in your firewall", default `:34` "JS_RTP_PORT_RANGE=10000-10200". **Unstated dependency:** a Docker host pulling a registry image, `MIXER docker-compose.yml:14` "image: ghcr.io/johnlegionh/legion-voice-mixer:latest" (doc: `MIXER README.md:31` "You need a host with **Docker**"). **Total:** 2 INI sections and 7 keys for working voice (9 for the permission model), 3 mixer env vars, a container runtime, and 4 manual steps (place the INI, create `.env`, start the container, open the firewall). |
| SC-109 | 8 | 166 | The small deployment has no allocator, no fleet, and no external dependencies beyond the mixer itself | OPS | | PARTIAL | **Met — no allocator or fleet:** one gateway per service, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:105` "janusConfig.GetString("JanusGatewayURI", string.Empty);", and one compose service, `MIXER docker-compose.yml:13` "janus:". `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. **Not met — external dependencies.** (1) A container runtime pulling a registry image, `MIXER docker-compose.yml:14` "image: ghcr.io/johnlegionh/legion-voice-mixer:latest". (2) A public STUN server by default, `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:26` "StunServers = stun:stun.l.google.com:19302", which stock viewers require (`:18`, comment). |
| SC-110 | 8 | 166 | Everything in §§3–7 works identically in the small mode | BOTH | | MET | **Met vacuously:** there is no separate small mode; one code path and one configuration model serve every deployment. The service takes a single gateway, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:105` "janusConfig.GetString("JanusGatewayURI", string.Empty);". No scale-out path exists that could behave differently: `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. **Not a verdict on §§3–7:** their own rows record what works. |
| SC-111 | 8 | 166 | Scale features activate by configuration, not by code path | BOTH | | PARTIAL | **Met — the one scale-related split is chosen by configuration.** Spatial and non-spatial services are named in config, `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:94` "moduleConfig.GetString("SpatialVoiceService", string.Empty);" and `:95` "moduleConfig.GetString("NonSpatialVoiceService", string.Empty);", and loaded by name, `:112` "ServerUtils.LoadPlugin<IWebRtcVoiceService>(spatialDllName, [m_Config]);". Either can be the grid connector: `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:7` "; SpatialVoiceService=WebRtcVoice.dll:WebRtcVoiceServiceConnector" and `:11`. **Not met — no other scale features exist to activate.** `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'placement\|shard\|bin.?pack\|mixer.?pool' -- src docker-compose.yml docker-entrypoint.sh env.sample etc` → 1 match, "replacement" in `MIXER docker-entrypoint.sh:191`. |
| SC-112 | 8.1 | 170 | One mixer process serves spatial + group + P2P | MIXER | | PARTIAL | **Met — spatial and P2P on one mixer.** The shipped INI names one service for both roles (`SIM Addons/os-webrtc-janus/os-webrtc-janus.ini:5`, `:9`), shared as one instance, `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:138` "m_nonSpatialVoiceService = m_spatialVoiceService;". Its rooms cover both channel types, `SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:247` "case "local":" and `:253` "case "multiagent":". **Not met — group voice.** Starting a conference is a stub, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/ChatSessionRequestLogic.cs:169` "case MethodStartConference:", under `:167` "Stubs carried over unchanged: 200, no body.". A multiagent provision is admitted only for a registered avatar-to-avatar session: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/A2AProvisionAdmission.cs:85` "A2ASession s = registry?.TryGetByChannel(channel);". |
| SC-113 | 8.1 | 170 | Small-deployment diagnostics are the console command and one status page | OPS | | PARTIAL | **Met — console commands:** `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:598` "AddCommand("Webrtc", false, "janus info"," and `:602` "AddCommand("Webrtc", false, "janus list rooms","; also `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceModerationCommands.cs:89` "AddCommand("Voice", false, "voice moderation mute",". **Not met — status page:** `git -C D:/legion-voice-mixer grep -n -i -E 'text/html\|<html\|status page\|dashboard' -- src etc connectors docker-entrypoint.sh docker-compose.yml Dockerfile` → 0 matches; `git -C D:/tranq-ais grep -n -i -E 'text/html\|<html\|status page\|dashboard' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-114 | 8.1 | 170 | STUN only; TURN optional | OPS | | PARTIAL | **Met — STUN only:** the sim advertises STUN servers to the viewer, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:236` "simFeatures?.AddFeature("stun-servers", OSD.FromString(m_StunServers));", with a STUN entry in the example, `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:26`. **Not met — TURN as an option:** neither side exposes a TURN setting. `git -C D:/tranq-ais grep -n -i -E 'turn:\|turns:\|TurnServers\|turn-servers\|turn_server' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'turn_server\|turn_rest\|turn_user\|turn_pwd\|turn_type' -- docker-entrypoint.sh env.sample docker-compose.yml etc` → 0 matches. |
| SC-115 | 8.1 | 170 | This must install with: build, one INI section, and nothing further | OPS | | UNMET | **Addressed by `3283c939f1` (fix V-1); not re-verdicted.** `PluginName` now defaults to `janus.plugin.slvoice`. The shipped INI and the example carry the four previously missing keys. The addon README install section lists the two INI sections, the mixer env vars and the Docker dependency. The evidence below is pass 1d's, for a later pass to re-check. **Met — the build step:** the region server builds the addon, `SIM Source/OpenSim.Server.RegionServer/OpenSim.Server.RegionServer.csproj:86` "<ProjectReference Include="../../Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.csproj" />". **Not met — "one INI section, and nothing further".** (1) Two sections are read, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:105` `[WebRtcVoice]` and `:125` `[JanusWebRtcVoice]`. (2) Voice is off as shipped, `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini:2` "Enabled=false". (3) `PluginName` is absent and defaults to the stock plugin, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:112` "GetString("PluginName", "janus.plugin.audiobridge")". The shipped-INI grep `'StunServers\|PluginName\|VisibilityFeederEnabled'` → 0 matches; `StunServers` is required by stock viewers (`SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:18`, comment). (4) The mixer needs a `.env` with three blank-shipped values, `MIXER env.sample:6`, `:28`, `:30`, and refuses to start without the secrets, `MIXER docker-entrypoint.sh:118` "exit 1". (5) A container runtime, `MIXER docker-compose.yml:14`. (6) An open UDP media range, `MIXER env.sample:33` "(comment) open this range in your firewall". SC-108 gives the full count. |
| SC-116 | 8.1 | 170 | "Voice works out of the box and tells you why when it doesn't" | BOTH | DERIVED | DERIVED | Pass-0b ruling: an umbrella goal like SC-7. The ruling names no rows; this pass reads the claim's two halves as deriving from SC-115 ("works out of the box") and from SC-49 and SC-113 ("tells you why"). |
| SC-117 | 8.2 | 174 | Spatial mixers scale horizontally by region | OPS | | PARTIAL | **Met — per simulator process:** each process's config names its own mixer, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:105` "_JanusServerURI = janusConfig.GetString("JanusGatewayURI", string.Empty);". Regions in separate processes can therefore use separate mixers. **Not met — per region, or by any mapping.** The URI is read once from the process's `[JanusWebRtcVoice]` section, `:102` "IConfig janusConfig = _Config.Configs["JanusWebRtcVoice"];", by a service loaded once, `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:112` "ServerUtils.LoadPlugin<IWebRtcVoiceService>(spatialDllName, [m_Config]);". `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-118 | 8.2 | 174 | The group/adhoc pool scales independently | OPS | | PARTIAL | **Met — the non-spatial service is configured separately.** Its name is read at `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:95` "moduleConfig.GetString("NonSpatialVoiceService", string.Empty);". A different name loads a separate instance, `:121` "if (spatialDllName != nonSpatialDllName)" → `:123`, and that instance can be the grid connector: `SIM Addons/os-webrtc-janus/os-webrtc-janus.ini.example:11` "; NonSpatialVoiceService=WebRtcVoice.dll:WebRtcVoiceServiceConnector". **Not met — no scaling within the pool.** A local Janus non-spatial service reads the same `[JanusWebRtcVoice]` gateway as the spatial one (`SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:102`–`:105`), and the grid side is again a single gateway. `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-119 | 8.2 | 174 | A simple placement map (region → mixer) replaces the single process | BOTH | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the scale tier is not needed for the target deployment. The descoping decision listed this item under §8.3; the row is §8.2. Pass 1d's finding, kept for the record: `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'placement\|shard\|bin.?pack\|mixer.?pool' -- src docker-compose.yml docker-entrypoint.sh env.sample etc` → 1 match, "replacement" in `MIXER docker-entrypoint.sh:191`. The mixer endpoint is one string: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:105`. |
| SC-120 | 8.2 | 174 | Adjacent regions preferentially co-located on one mixer | OPS | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the scale tier is not needed for the target deployment. The descoping decision listed this item under §8.3; the row is §8.2. Pass 1d's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'adjacent\|co.?locat\|neighbo' -- src docker-entrypoint.sh env.sample` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'adjacent\|co.?locat' -- Addons/os-webrtc-janus ':!*.md'` → 3 matches, all comments about child agents of neighbouring regions, none about placement: `SIM Addons/os-webrtc-janus/WebRtcVoice/VoiceViewerSession.cs:73`, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/FeederWorldFromScene.cs:58` and `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:250`. |
| SC-121 | 8.2 | 174 | Cross-region listening via multiple neighbor sessions summed client-side per the protocol | BOTH | | MET | **Neighbour sessions accepted:** the sim admits a child agent's "local" provision, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:669` "new ParcelResolveInput(clientParcelId, sp.IsChildAgent);". Without a client parcel id it skips the parcel checks, `:690` "if (res.Source != ParcelSource.None)" with `:688`–`:689` "(comment) the service defaults to the estate room. The normal neighbour-region path.", and joins that region's estate room: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:385` "? pli : JanusAudioBridge.REGION_ROOM_ID;". **Closed together:** a quit closes every region's session, `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:301` "VoiceViewerSession.CaptureSessionsForCloseAllRegions(pAgentId, pGeneration, pRootRegionsElsewhere);". **Client-side summing:** each connection gets its own mix, `MIXER src/janus_slvoice.c:3175` "summed = slv_mix_nminus1_stereo(frame, SLV_FRAME_TOTAL, srcbuf, audible,"; the summing itself is viewer behaviour. **Note:** the child-agent parcel hint (SC-80). |
| SC-122 | 8.3 | 178 | Placement service: bin-packing regions onto mixers by predicted load | OPS | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'placement\|shard\|bin.?pack\|mixer.?pool' -- src docker-compose.yml docker-entrypoint.sh env.sample etc` → 1 match, "replacement" in `MIXER docker-entrypoint.sh:191`. |
| SC-123 | 8.3 | 178 | Sessions carry enough state to migrate | BOTH | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: `git -C D:/tranq-ais grep -n -i -E 'migrat' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. `git -C D:/legion-voice-mixer grep -n -i -E 'migrat' -- src` → 1 match, a comment on the code's history: `MIXER src/mixer/mixer.h:13` "a risky migration of the field-proven". No export or import of session state exists in either tree. |
| SC-124 | 8.3 | 179 | Admission control with backpressure: saturated mixers shed new sessions to other capacity; established sessions are protected | BOTH | | PARTIAL | **Met — capacity is enforced at join, so established sessions keep their places.** `MIXER src/janus_slvoice.c:2262` "if(room_pop >= SLV_MAX_MIX) {" → `:2264` "error_code = JANUS_SLVOICE_ERROR_ROOM_FULL;". The refusal reaches the viewer as HTTP 409: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:760` "provErrorCode == WebRtcJanusService.JANUS_ROOM_FULL_ERROR_CODE)" → `:761` "response.StatusCode = (int)HttpStatusCode.Conflict;". **Not met — shedding to other capacity.** A refused session has nowhere else to go, because there is one gateway, `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:105`, and no placement: `git -C D:/tranq-ais grep -n -i -E 'placement\|shard\|bin.?pack\|MixerPool\|mixer.?pool\|GatewayURIs' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. "Saturated" is also a participant count, not load; tick overruns are only counted, `MIXER src/janus_slvoice.c:3217` "room->tick_overruns++;". |
| SC-125 | 8.3 | 180 | TURN fleet as its own capacity plan | OPS | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: OPS row, so docs count as evidence here, but none holds a TURN capacity plan. `git -C D:/tranq-ais grep -n -i -E '\bTURN\b' -- Docs/voice` → 6 matches, none a plan: the spec's own claim lines (`webrtc-voice-spec.md:170`, `:180`), this document's SC-114 and SC-125 rows, the ledger's absence entry (`voice-programme-ledger.md:159`), and an unrelated "turn" in `per-room-visibility-emission-design-brief.md:366`. `git -C D:/legion-voice-mixer grep -n -i -E '\bTURN\b' -- docs README.md` → 4 matches, the mirrored copies of that brief, ledger and spec. **No TURN in config:** `git -C D:/legion-voice-mixer grep -n -i -E 'turn_server\|turn_rest\|turn_user\|turn_pwd\|turn_type' -- docker-entrypoint.sh env.sample docker-compose.yml etc` → 0 matches; `git -C D:/tranq-ais grep -n -i -E 'turn:\|turns:\|TurnServers\|turn-servers\|turn_server' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-126 | 8.3 | 180 | The diag vector (§4.1) distinguishes relay-path from direct-path sessions | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'srflx\|prflx\|relay_ratio\|candidate_type\|selected_pair\|turn_used\|"relay"' -- src docker-entrypoint.sh etc` → 0 matches. |
| SC-127 | 8.3 | 181 | Optional first-order ambisonic delivery: orientation-independent B-format mixes, rotated and binauralized viewer-side | VIEWER | | BLOCKED | Rotating and binauralizing a B-format mix in the viewer is a viewer change. Constraint: stock SL/Firestorm viewer compatibility; viewer changes only when unavoidable, and then fork-only. |
| SC-128 | 8.3 | 181 | First-order ambisonic delivery is SDP-negotiated with fallback to server-side binaural stereo for stock viewers | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'ambisonic\|b.?format\|\bfoa\b\|spherical.?harm' -- src` → 0 matches. Every listener gets the stereo encoder: `MIXER src/janus_slvoice.c:2699` "s->enc = opus_encoder_create(SLV_RATE, SLV_CHANNELS, OPUS_APPLICATION_VOIP, &err);". |
| SC-129 | 8.3 | 181 | B-format mixes dedupe across co-located listeners | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'ambisonic\|b.?format\|\bfoa\b\|spherical.?harm' -- src` → 0 matches. Each listener gets its own mix and encode: `MIXER src/janus_slvoice.c:3175` "summed = slv_mix_nminus1_stereo(" and `:3203` "janus_slvoice_encode_relay(s, frame, SLV_FRAME_SAMPLES, silence);". |
| SC-130 | 8.3 | 181 | The ambisonic delivery mitigation is wired in early (brutal to retrofit) | MIXER | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: `git -C D:/legion-voice-mixer grep -n -i -E 'ambisonic\|b.?format\|\bfoa\b\|spherical.?harm' -- src` → 0 matches. **Pass-0b ruling:** deferred by decision; its status is a decision John owes rather than a defect. |
| SC-131 | 8.3 | 182 | Abuse controls: rate-limited provisioning | SIM | | PARTIAL | **Met — refused provisions are throttled.** A repeat of a refusal for the same (agent, region) is answered from cache before any check runs, `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:620` "m_refusalCache.TryGetRefusal(agentID, scene.RegionInfo.RegionID, out object cachedRefusal)". The window is configurable: `:114` "m_Config.GetInt("RefusalCacheSeconds", ProvisionRefusalCache.DefaultSeconds)". **Not met — successful provisions are not limited;** the cache holds only refusals, `:68` "(comment) replays a provision refusal (403/404/501)". `git -C D:/tranq-ais grep -n -i -E 'rate.?limit\|throttle\|MaxSessions\|session.?cap\|per.?account\|max.?per' -- Addons/os-webrtc-janus ':!*.md'` → 2 matches: the refusal-throttle comment at `:514`, and `VoiceViewerSession.CaptureSessionsForCloseAllRegions` at `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:301`, which captures sessions at close time. |
| SC-132 | 8.3 | 182 | Abuse controls: per-account session caps | SIM | | DESCOPED | **DESCOPED 2026-09-14 (a decision, not a finding):** the large-grid scale tier is not needed for the target deployment. Pass 1d's finding, kept for the record: `git -C D:/tranq-ais grep -n -i -E 'MaxSessions\|session.?cap\|sessions.?per\|per.?account\|SessionLimit' -- Addons/os-webrtc-janus ':!*.md'` → 1 match, not a cap: the close-time capture `VoiceViewerSession.CaptureSessionsForCloseAllRegions` at `SIM Addons/os-webrtc-janus/WebRtcVoiceServiceModule/WebRtcVoiceServiceModule.cs:301`. The only cap is per room, not per account: `MIXER src/janus_slvoice.c:2262` "if(room_pop >= SLV_MAX_MIX) {". |
| SC-133 | 8.3 | 182 | Abuse controls: RTP source validation | MIXER | | MET | **Authenticated before the plugin sees it:** RTP reaches the plugin only after the Janus core checks it against the PeerConnection's DTLS-SRTP keys, `MIXER vendor/janus-gateway/src/ice.c:2704` "srtp_unprotect(pc->dtls->srtp_in, buf, &buflen) : srtp_err_status_ok;". A failure takes the error branch, `:2705` "if(res != srtp_err_status_ok) {", and the packet is handled only in the `else` at `:2712`. Encryption is on unless `no_webrtc_encryption` is set (`MIXER vendor/janus-gateway/src/janus.c:4966`), and the image never sets it: `git -C D:/legion-voice-mixer grep -n -i -E 'webrtc_encryption\|no_webrtc' -- docker-entrypoint.sh etc env.sample docker-compose.yml` → 0 matches. **Bound to its own handle:** the plugin takes a packet only on the session's own handle, `MIXER src/janus_slvoice.c:3335` "janus_slvoice_session *session = (janus_slvoice_session *)handle->plugin_handle;", and drops video, `:3338` "if(packet == NULL \|\| packet->buffer == NULL \|\| packet->video)". |
| SC-134 | 9 | 188 | Caps: `ProvisionVoiceAccountRequest` (JSEP offer/answer, `channel_type` local/multiagent, `parcel_local_id`, logout) | SIM | | MET | **Cap:** registered at `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:328` "caps.RegisterSimpleHandler("ProvisionVoiceAccountRequest",". **JSEP offer:** `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:399` "if (pRequest.TryGetOSDMap("jsep", out OSDMap jsep))" → `:404` "if (jsepType == "offer")". **Answer:** `SIM Addons/os-webrtc-janus/Janus/ProvisionResponseBuilder.cs:25` "{ "jsep", jsepAnswer },". **`channel_type`:** both types, `SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:247` "case "local":" and `:253` "case "multiagent":". **`parcel_local_id`:** `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:385` "pRequest.TryGetInt("parcel_local_id", out int pli)". **Logout:** `:365` "bool isLogout = pRequest.TryGetBool("logout", out bool lgout) && lgout;". |
| SC-135 | 9 | 188 | Caps: `VoiceSignalingRequest` (trickled ICE, completion marker) | SIM | | PARTIAL | **Met — the cap and the completion marker.** Registered at `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:334` "caps.RegisterSimpleHandler("VoiceSignalingRequest","; completion is handled at `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:507` "if (candidate.TryGetBool("completed", out bool iscompleted) && iscompleted)". **Not met — trickled candidates do not reach Janus.** They are handed on at `:545` "viewerSession.Session.TrickleCandidates(viewerSession, candidatesArray)", but that method sends the one-argument request, `SIM Addons/os-webrtc-janus/Janus/JanusSession.cs:200` "SendToJanusNoWait(new TrickleReq(pVSession));" (and `:204`). That constructor builds the end-of-candidates message, `SIM Addons/os-webrtc-janus/Janus/JanusMessages.cs:329` "public TrickleReq(JanusViewerSession pVSession) : base("trickle")" → `:333` "{ "completed", true },". Ledger O-79. |
| SC-136 | 9 | 189 | SDP: fmtp mangle honored (`minptime=10;useinbandfec=1;stereo=1;sprop-stereo=1;maxplaybackrate=48000`) | MIXER | | MET | The answer carries that exact fmtp: `MIXER src/janus_slvoice.c:2043` "minptime=10;useinbandfec=1;stereo=1;sprop-stereo=1;maxplaybackrate=48000". **Note:** advertising `minptime=10` while the mix reads 20 ms frames is ledger O-81. |
| SC-137 | 9 | 190 | SLData channel client→mixer `j/l/sp/sh/lp/lh/m/ug` per the published format | MIXER | | MET | Every member is parsed in `MIXER src/sldata.c`: `:128` "json_object_get(root, "j")", `:130` "json_object_get(root, "l")", `:134` "json_object_get(root, "sp")", `:138` "json_object_get(root, "sh")", `:142` "json_object_get(root, "lp")", `:146` "json_object_get(root, "lh")", `:158` "json_object_get(root, "m")", `:184` "json_object_get(root, "ug")". **Note:** for `j` and `l` only their presence is recorded (`:126`–`:127`, comment); see SC-140. |
| SC-138 | 9 | 190 | SLData channel mixer→client per-peer `p/V/j/l` batched ~100 ms | MIXER | | PARTIAL | **Cadence:** a fixed 100 ms sleep between passes, `MIXER src/janus_slvoice.c:113` "#define SLV_POWER_TICK_MS   100", slept at `:2516` "g_usleep(SLV_POWER_TICK_MS * 1000);". The sleep is not clock-scheduled, so the real interval is 100 ms plus the time to walk every room. **Met — `p` and the VAD flag, batched per room:** `:2585` "json_object_set_new(pv, "p", json_integer(ppow));" and `:2586` "json_object_set_new(pv, "v", pvad ? json_true() : json_false());". The key is lowercase `v`, not the spec's `V`; the source comment records that the viewer reads `v` (`:2541`). **Not met — `j` and `l` are not in the batch.** They are sent at the moment of the event: join presence `:852` "json_object_set_new(jd, "p", json_true());" → `:853` "json_object_set_new(sub, "j", jd);", relayed immediately at `:829` "gateway->relay_data(s->handle, &data);". |
| SC-139 | 9 | 190 | SLData extensions, all optional and ignored by stock viewers: `diag` (§4.1), echo-test control (§4.2), morph state (§7.4), trust-domain disclosure (§3.2) | MIXER | | PARTIAL | **Met — echo-test control:** an optional client field, `MIXER src/sldata.c:213` "slv_get_bool(json_object_get(root, "echo"), &b)". **Not met — `diag`, morph state and trust-domain disclosure:** `git -C D:/legion-voice-mixer grep -n -i -E '"diag"\|trust.?domain\|morph' -- src` → 0 matches. |
| SC-140 | 9 | 191 | Cross-region: neighbor connections with primary flag, client-side summing, per the published model | BOTH | | PARTIAL | **Met — neighbour connections.** A child agent's provision is accepted into that region's estate room: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:669` "new ParcelResolveInput(clientParcelId, sp.IsChildAgent);", `:690` "if (res.Source != ParcelSource.None)", and `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:385` "? pli : JanusAudioBridge.REGION_ROOM_ID;". Each connection gets its own mix for the client to sum: `MIXER src/janus_slvoice.c:3175`. **Not met — the primary flag is never read.** The parser records only that `j` appeared, `MIXER src/sldata.c:128` "if(json_object_get(root, "j") != NULL)", and its inner shape is not decoded (`:126`–`:127`, comment). `git -C D:/legion-voice-mixer grep -n -i -E 'primary\|"p"' -- src/sldata.c src/sldata.h` → 0 matches. `git -C D:/tranq-ais grep -n -i -E 'primary' -- Addons/os-webrtc-janus ':!*.md'` → 0 matches. |
| SC-141 | 9 | 191 | Parcel changes within a region do not trigger connection changes (§7.3) | BOTH | | PARTIAL | Filled in pass 1c alongside SC-88. It is the same claim on the same code path, and gets the same result. **Met — estate-channel parcels.** A crossing only marks the visibility matrix dirty: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/VoiceVisibilityService.cs:221` "m_feeder.OnAvatarEnteringNewParcel();" → `SIM Addons/os-webrtc-janus/Visibility/VoiceStateFeeder.cs:64` "public void OnAvatarEnteringNewParcel() => Invalidate();". Both parcels map to the estate room: `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:716` "map.Remove("parcel_local_id"); // estate channel" and `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:385` "? pli : JanusAudioBridge.REGION_ROOM_ID;". **Not met — parcels with their own voice channel.** The room number hashes the parcel, `SIM Addons/os-webrtc-janus/Janus/JanusAudioBridge.cs:251` "hasher.Add(pParcelLocalID);", with the id set at `SIM Addons/os-webrtc-janus/WebRtcVoiceRegionModule/WebRtcVoiceRegionModule.cs:722`. The mixer cannot move a participant between rooms: `git -C D:/legion-voice-mixer grep -n -i -E 'changeroom\|change_room\|switch_room\|"move"' -- src` → 0 matches. The sim does not itself trigger the change, but reaching the new parcel's room takes a new provision and join: `SIM Addons/os-webrtc-janus/Janus/WebRtcJanusService.cs:408` "viewerSession.Room = await viewerSession.AudioBridge.SelectRoom(pSceneID.ToString(),". |

## Flag rulings (John, 2026-09-14)

Every row flagged in pass 0, with the ruling now in its Flag column, the ruling's reason, and where set,
the criterion pass 1 must apply. Rulings are final.

| ID | Ruling | Reason / pass-1 criterion |
|---|---|---|
| SC-7 | DERIVED | Umbrella goal with no separate finding of its own. It derives from SC-41 (the admin web view) and the §4.4 session event log claims (SC-52, SC-53). |
| SC-23 | blank | Criterion: does the provisioning response carry the trust-domain classification as a field the client can read? |
| SC-26 | blank | Criterion: is any disclosure text or flag emitted to local users? |
| SC-30 | blank | Criterion: does the sim→mixer state carry a version epoch? |
| SC-31 | blank | Criterion: when the state is stale or none has arrived, does the mixer withhold audio rather than pass it? |
| SC-32 | blank | The "§6.1" cross-reference is a spec error; read it as §7.1 (camera-position listening). Criterion: is client-reported position used anywhere as a permission or authorization input? |
| SC-38 | blank | Criterion: is capture consent-gated at the hook, per §3.5? |
| SC-41 | blank | The criterion is literal: a per-region and per-mixer web view. An admin API or console command is not a web view; pass 1 records what it finds either way. |
| SC-49 | blank | Criterion: does the mixer emit anything on the data channel when it cannot deliver audio a listener should be receiving? |
| SC-53 | blank | Criterion: any bounded short-term retention of the session event log. |
| SC-54 | blank | Criterion: are the four named metrics (per-mixer health, session counts, mix-deadline overrun rates, relay-vs-direct path ratios) found anywhere? |
| SC-58 | blank | Criterion: the configured or negotiated Opus ingest bitrate floor. |
| SC-65 | blank | Not superseded. A real claim about load-adaptive degradation. |
| SC-68 | blank | Same family as SC-65: a real claim about load-adaptive behaviour. |
| SC-72 | blank | "10 ms frames" is a spec error against §5's 20 ms (SC-60); read it as "several frames". Criterion: are there crossfades at bin crossings at all? |
| SC-79 | blank | An ordinary claim. John points pass 1 at the Phase 3b camera leash (`SLV_LEASH_DIST`). |
| SC-83 | BLOCKED-BY-CONSTRAINT | Needs a viewer-side change. The standing constraint is stock SL/Firestorm compatibility, with viewer changes only when unavoidable and fork-only. Pass 1 records this row as blocked by that constraint. |
| SC-85 | AMENDED | The mechanism changed to per-parcel rooms with an estate-room fallback. The invariant (per-listener, enforced in the mix) stands and is still binding. |
| SC-88 | blank | Not superseded. The claim stands as written and is the one to test hardest: per-parcel rooms mean a parcel crossing is a room change, which is the connection-topology behaviour this claim says was removed. |
| SC-95 | AMENDED | Tap-and-inject takes the form of aiortc WebRTC peers joining a room normally, not plain RTP or WebSocket. Plain-RTP connectors were deferred by decision. |
| SC-98 | NOT-A-COMMITMENT | The spec itself says "future translation". |
| SC-108 | blank | A real claim about the small-deployment default case; not superseded. |
| SC-115 | blank | Same as SC-108. Pass 1 tests it honestly against what installing actually requires today. |
| SC-116 | DERIVED | Umbrella goal, like SC-7, with no separate finding of its own. |
| SC-130 | blank | A real claim, deferred by decision. The spec's own rationale is that retrofitting is expensive, so its status is a decision John owes rather than a defect. |
| SC-138 | blank | A real claim. Pass 1 measures the actual cadence against "~100 ms". |
| SC-141 | blank | Not superseded. Same family as SC-88 and equally binding. |

### Wording notes

These adjustments are not meant to change meaning:
- **SC-19:** the spec's verb phrase before "as two-party sessions" is omitted.
- **SC-92:** the spec's verb before "PSOLA in-house" is rendered "write".
- **SC-115:** the spec's final word after "one INI section" is rendered "and nothing further".

## Counts

### Per section

| Spec § | Claims | IDs |
|---|---|---|
| Header (line 4) | 2 | SC-1 – SC-2 |
| 1 Goals and non-goals | 14 | SC-3 – SC-16 |
| 2 Requirements provenance | 0 | — |
| 3.1 All media is server-relayed | 5 | SC-17 – SC-21 |
| 3.2 Voice-server trust domains | 7 | SC-22 – SC-28 |
| 3.3 Sim-authoritative permission enforcement | 4 | SC-29 – SC-32 |
| 3.4 Mixer output is the permission boundary | 3 | SC-33 – SC-35 |
| 3.5 Capture requires consent | 3 | SC-36 – SC-38 |
| 4.1 Per-connection state vector | 5 | SC-39 – SC-43 |
| 4.2 Built-in echo test | 5 | SC-44 – SC-48 |
| 4.3 Fail loud | 3 | SC-49 – SC-51 |
| 4.4 Session event log | 2 | SC-52 – SC-53 |
| 4.5 Fleet observability | 2 | SC-54 – SC-55 |
| 5 Media plane quality | 11 | SC-56 – SC-66 |
| 6 Spatial engine | 11 | SC-67 – SC-77 |
| 7.1 Camera-position listening | 3 | SC-78 – SC-80 |
| 7.2 Performer mode | 4 | SC-81 – SC-84 |
| 7.3 Parcel and estate voice zones | 4 | SC-85 – SC-88 |
| 7.4 Voice morphing | 6 | SC-89 – SC-94 |
| 7.5 Connector layer | 7 | SC-95 – SC-101 |
| 7.6 Moderation surface | 4 | SC-102 – SC-105 |
| 8 Deployment and scaling model | 6 | SC-106 – SC-111 |
| 8.1 Small | 5 | SC-112 – SC-116 |
| 8.2 Medium | 5 | SC-117 – SC-121 |
| 8.3 Large | 12 | SC-122 – SC-133 |
| 9 Protocol compatibility summary | 8 | SC-134 – SC-141 |
| 10 Open questions | 0 | — (see the appendix) |
| **Total** | **141** | SC-1 – SC-141 |

### Per Scope

| Scope | Claims |
|---|---|
| MIXER | 57 |
| SIM | 11 |
| BOTH | 44 |
| OPS | 24 |
| VIEWER | 5 |
| **Total** | **141** |

### Per flag ruling

| Flag | Claims | IDs |
|---|---|---|
| blank | 135 | all others |
| AMENDED | 2 | SC-85, SC-95 |
| BLOCKED-BY-CONSTRAINT | 1 | SC-83 |
| DERIVED | 2 | SC-7, SC-116 |
| NOT-A-COMMITMENT | 1 | SC-98 |
| **Total** | **141** | |

### Pass 1a verdicts (§1, §3, §4: SC-3 – SC-55)

| Section | MET | PARTIAL | UNMET | BLOCKED | DERIVED | UNKNOWN | Rows |
|---|---|---|---|---|---|---|---|
| §1 (SC-3 – SC-16) | 9 | 3 | 1 | 0 | 1 | 0 | 14 |
| §3 (SC-17 – SC-38) | 6 | 6 | 10 | 0 | 0 | 0 | 22 |
| §4 (SC-39 – SC-55) | 2 | 5 | 7 | 3 | 0 | 0 | 17 |
| **Total** | **17** | **14** | **18** | **3** | **1** | **0** | **53** |

### Pass 1b verdicts (§5, §6: SC-56 – SC-77)

| Section | MET | PARTIAL | UNMET | BLOCKED | DERIVED | UNKNOWN | Rows |
|---|---|---|---|---|---|---|---|
| §5 (SC-56 – SC-66) | 7 | 2 | 2 | 0 | 0 | 0 | 11 |
| §6 (SC-67 – SC-77) | 3 | 2 | 6 | 0 | 0 | 0 | 11 |
| **Total** | **10** | **4** | **8** | **0** | **0** | **0** | **22** |

### Pass 1c verdicts (§7: SC-78 – SC-105, plus SC-141)

| Section | MET | PARTIAL | UNMET | BLOCKED | DERIVED | UNKNOWN | NOT-A-COMMITMENT | Rows |
|---|---|---|---|---|---|---|---|---|
| §7.1 (SC-78 – SC-80) | 1 | 2 | 0 | 0 | 0 | 0 | 0 | 3 |
| §7.2 (SC-81 – SC-84) | 0 | 0 | 3 | 1 | 0 | 0 | 0 | 4 |
| §7.3 (SC-85 – SC-88) | 2 | 2 | 0 | 0 | 0 | 0 | 0 | 4 |
| §7.4 (SC-89 – SC-94) | 0 | 0 | 6 | 0 | 0 | 0 | 0 | 6 |
| §7.5 (SC-95 – SC-101) | 1 | 3 | 2 | 0 | 0 | 0 | 1 | 7 |
| §7.6 (SC-102 – SC-105) | 2 | 1 | 1 | 0 | 0 | 0 | 0 | 4 |
| **§7 total** | **6** | **8** | **12** | **1** | **0** | **0** | **1** | **28** |
| §9, SC-141 only | 0 | 1 | 0 | 0 | 0 | 0 | 0 | 1 |
| **Pass 1c total** | **6** | **9** | **12** | **1** | **0** | **0** | **1** | **29** |

### Pass 1d verdicts (§8, §9: SC-106 – SC-140)

| Section | MET | PARTIAL | UNMET | BLOCKED | DERIVED | UNKNOWN | NOT-A-COMMITMENT | Rows |
|---|---|---|---|---|---|---|---|---|
| §8 (SC-106 – SC-111) | 3 | 2 | 1 | 0 | 0 | 0 | 0 | 6 |
| §8.1 (SC-112 – SC-116) | 0 | 3 | 1 | 0 | 1 | 0 | 0 | 5 |
| §8.2 (SC-117 – SC-121) | 1 | 2 | 2 | 0 | 0 | 0 | 0 | 5 |
| §8.3 (SC-122 – SC-133) | 1 | 2 | 8 | 1 | 0 | 0 | 0 | 12 |
| §9 (SC-134 – SC-140; SC-141 in pass 1c) | 3 | 4 | 0 | 0 | 0 | 0 | 0 | 7 |
| **Pass 1d total** | **8** | **13** | **12** | **1** | **1** | **0** | **0** | **35** |

## Whole-document summary

141 rows in total, all with a verdict. The counts are as of the spec amendment of 2026-09-14.

| Verdict | Rows |
|---|---|
| MET | 44 |
| PARTIAL | 39 |
| UNMET | 21 |
| DESCOPED | 29 |
| BLOCKED | 5 |
| DERIVED | 2 |
| NOT-A-COMMITMENT | 1 |
| UNKNOWN | 0 |
| No verdict | 0 |
| **Total** | **141** |

**By pass:**

| Pass | Rows verdicted or changed |
|---|---|
| 1a | 53 |
| 1b | 22 |
| 1c | 29 |
| 1d | 35 |
| Spec amendment | 32 (29 to `DESCOPED`, SC-68 to `MET`, SC-1 and SC-2 first verdicted) |

**Changes made by the spec amendment:**

| Rows | From | To |
|---|---|---|
| SC-65, SC-70, SC-71, SC-72, SC-74, SC-75, SC-77, SC-81, SC-82, SC-84, SC-89 – SC-94, SC-97, SC-101, SC-103, SC-119, SC-120, SC-122, SC-123, SC-125, SC-126, SC-128, SC-129, SC-130, SC-132 | `UNMET` | `DESCOPED` |
| SC-68 | `PARTIAL` | `MET` (load-adaptive half descoped) |
| SC-1, SC-2 | no verdict | `MET` (SC-2: large multi-mixer end descoped) |

The pass tables above are left as each pass recorded them. This summary is the current state.

## Appendix — §10 open questions carried from the spec

These are unresolved questions carried from the spec, not claims. They are not inventoried and not
answered here.

| Line | Question (verbatim) |
|---|---|
| 195 | 1. Hypergrid group/P2P policy: which grid's pool hosts a call between users of two federated grids, and what does each party's client disclose? |
| 196 | 2. FOA viewer-side decode: target viewer(s) and negotiation details; who carries the viewer patch. |
| 197 | 3. Recording/consent defaults per jurisdiction for the connector layer. |
| 198 | 4. Session migration mechanics for live mixer drain at scale (needed for §8.3; over-engineering for §8.1 — gate behind the placement service). |
