# --- C / plugin build artifacts ---
*.o
*.lo
*.a
*.la
*.so
*.so.*
*.d
src/*.o
janus_slvoice.so
# compiled unit-test binaries (built by `make test`)
tests/test_sldata
tests/test_mix
tests/test_visbatch
tests/test_roster

# --- Secrets / local config (NEVER commit; use env.sample as the template) ---
.env
secrets

# --- Editor / OS cruft ---
*.swp
*~
.DS_Store
.vscode/
.idea/

# --- Local build output dirs / logs ---
/build/
/out/
*.log
.build.log

# Note: vendor/janus-gateway is a git submodule (a tracked gitlink), not
# ignored. Its own working-tree build artifacts are covered by its .gitignore.

# S-CON-4 recorder output (compose volume ./recordings)
recordings/
